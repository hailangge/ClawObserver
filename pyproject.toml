[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clawobserver"
version = "0.1.0"
description = "Lightweight runtime-first monitoring for OpenClaw."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["clawobserver*"]

