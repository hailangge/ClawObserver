# Tasks

## In progress

- [x] Synchronize the release/deployment requirements and design docs for the final operator-facing finish: spec docs live under `specification/`, README coverage expands to a full operator manual, and deployment explicitly requires safe/idempotent install plus safe uninstall (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Move the root-level product docs into `specification/` and update repo references so the repo no longer relies on root-level `requirements.md`, `design.md`, or `tasks.md` (`README.md`, tests, spec docs)
- [x] Replace the one-way deploy wrapper with robust user-level install/uninstall scripts that render units/env with repo and home substitutions, preserve env/state by default, and print operator management commands (`scripts/install.sh`, `scripts/uninstall.sh`, `scripts/systemd_user_common.sh`, wrappers, `deploy/systemd/clawobserver.env.example`)
- [x] Rewrite `README.md` as the complete operator/developer manual covering install, configuration, service/timer management, manual capture, uninstall, development, validation, architecture, data paths, troubleshooting, and provenance notes (`README.md`)
- [x] Add practical automated coverage for install/uninstall rendering behavior and the new specification-doc location contract, then run shell syntax and Python validation and record exact evidence here (`tests/test_deploy_scripts.py`, `tests/test_specification_docs.py`, validation)

- 2026-05-13 release/deployment finishing sync: `specification/requirements.md` now requires a documented install script, a documented uninstall script, idempotent env-preserving reruns, default-safe uninstall semantics, complete README operator coverage, and the canonical spec-doc location under `specification/`. `specification/design.md` now describes install/uninstall behavior explicitly instead of only a generic deploy wrapper, and its README expectations now cover configuration, manual capture, uninstall, validation, data paths, and provenance notes.
- 2026-05-13 install/uninstall implementation: added `scripts/systemd_user_common.sh` as the shared path/render helper, `scripts/install.sh` as the primary operator install entrypoint, and `scripts/uninstall.sh` as the matching user-level uninstall flow. `scripts/deploy.sh` and `scripts/install_user_service.sh` now forward to `scripts/install.sh` for compatibility. The install path rerenders units against the current repo checkout, preserves an existing `~/.config/clawobserver/clawobserver.env`, runs `systemctl --user daemon-reload`, and enables/starts `clawobserver.service` plus `clawobserver-capture.timer`. The uninstall path stops/disables units, removes rendered user unit files, runs `daemon-reload` plus `reset-failed`, preserves config/state by default, and only removes them when `--purge-config`, `--purge-state`, or `--purge-all` is passed.
- 2026-05-13 README/operator-doc completion: `README.md` now documents installation, configuration, installed paths, service/timer management, manual capture, uninstall semantics, development workflow, validation commands, archive cadence semantics, runtime adapter behavior, architecture, and asset/provenance locations. Root-level product docs were moved to `specification/requirements.md`, `specification/design.md`, and `specification/tasks.md`, and README/test references were updated accordingly.
- 2026-05-13 automated coverage extension: `tests/test_deploy_scripts.py` now executes install/uninstall against a temp `HOME` with a stub `systemctl`, verifies rendered unit/env contents, idempotent env preservation, default-safe uninstall preservation, and destructive purge mode. `tests/test_specification_docs.py` now asserts the canonical `specification/` doc paths exist, the old root-level files are gone, and the README points at the new paths.
- 2026-05-13 validation evidence for the release/deployment finish:
  `bash -n scripts/install.sh scripts/uninstall.sh scripts/deploy.sh scripts/install_user_service.sh scripts/systemd_user_common.sh` => passed.
  `python3 -m pytest tests/test_deploy_scripts.py tests/test_specification_docs.py` => passed (`7` tests).
  `python3 -m pytest` => passed (`66` tests).
  Frontend tests/build/browser smoke were not rerun for this pass because the changes were limited to shell scripts, repo docs, and Python-side tests; no frontend/runtime asset code changed in this release-finishing work.

- **Independent verification on 2026-05-13 (Kimi Code CLI) — release/deployment finishing changes**: independently verified the install/uninstall scripts, README completion, specification doc migration, and automated coverage.
  - Specification doc migration review: root-level `requirements.md`, `design.md`, and `tasks.md` are deleted (`D` in `git status`). Canonical specs live under `specification/requirements.md` (375 lines), `specification/design.md` (588 lines), and `specification/tasks.md` (398 lines). `README.md` points to all three canonical paths. `tests/test_specification_docs.py` asserts the canonical paths exist, the root-level files are gone, and the README references them. No stray references to root-level spec docs remain in `clawobserver/`, `scripts/`, `deploy/`, `tests/`, `pyproject.toml`, `.gitignore`, or `docs/`.
  - Install script review: `scripts/install.sh` sources `scripts/systemd_user_common.sh`, calls `install_rendered_template` for `clawobserver.service`, `clawobserver-capture.service`, and `clawobserver.env` (only when absent), calls `install_file_if_changed` for `clawobserver-capture.timer`, preserves an existing `~/.config/clawobserver/clawobserver.env` on reruns, runs `systemctl --user daemon-reload`, and enables/starts both `clawobserver.service` and `clawobserver-capture.timer`. It prints the local access URL plus status and management commands. Wrapper compatibility confirmed: `scripts/deploy.sh` and `scripts/install_user_service.sh` both `exec` forward to `scripts/install.sh`.
  - Uninstall script review: `scripts/uninstall.sh` stops and disables `clawobserver-capture.timer`, `clawobserver.service`, and the one-shot `clawobserver-capture.service`; removes all three rendered unit files; runs `daemon-reload` and `reset-failed`; preserves `~/.config/clawobserver/clawobserver.env` and `~/.local/state/clawobserver/` by default; and only purges config/state when `--purge-config`, `--purge-state`, or `--purge-all` is explicitly passed. `remove_if_present` and `cleanup_dir_if_empty` prevent errors on absent paths.
  - Env/timer coherence review: `deploy/systemd/clawobserver.env.example` defaults `CLAWOBSERVER_REFRESH_SECONDS=30` and `CLAWOBSERVER_ARCHIVE_CADENCE_MINUTES=30`, matching the frontend minimum auto-refresh floor and the timer's `OnCalendar=*:0/30`. README sections on archive cadence, timer management, and data paths are consistent with the template and timer file.
  - Automated coverage review: `tests/test_deploy_scripts.py` verifies rendered unit/env contents against a stub `systemctl`, idempotent env preservation, default-safe uninstall preservation, and destructive `--purge-all` mode. `tests/test_specification_docs.py` verifies the spec doc location contract and README references. Both files pass.
  - Commands run and results:
    - `bash -n scripts/install.sh scripts/uninstall.sh scripts/deploy.sh scripts/install_user_service.sh scripts/systemd_user_common.sh` => all passed.
    - `python3 -m pytest tests/test_deploy_scripts.py tests/test_specification_docs.py` => `7 passed`.
    - `python3 -m pytest` => `66 passed`.
    - `node --check clawobserver/static/app.js` => passed.
    - `python3 -m compileall clawobserver/` => passed.
    - `grep` sweeps for stray root-level spec-doc references across Python, shell, markdown, JS, HTML, JSON, TS, and CSS files => zero stray matches outside expected files.
  - Verdict: **PASS**. All release/deployment finishing changes are coherent, safe, idempotent, and fully covered by automated tests. No regressions detected. Existing uncommitted product/UI work remains intact.

- [x] Update root planning docs first for the focused 2026-05-13 WorkAdventure realtime repair so the product contract explicitly requires broader work-area overlays, larger readable marker/nameplate text, no visible auto-refresh remount cadence, and automated guards against rapid refresh / repeated asset fetches (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Expand the primary WorkAdventure work-area zone geometry to match the actual broader workspace on the approved custom background while preserving the existing lounge/rest correctness contract (`frontend/office-scene-prototype/src/data/workAdventurePreviewCatalog.ts`, `src/config/workAdventureLayout.ts`, `src/lib/sceneState.ts`, `src/components/WorkAdventureScene.tsx`)
- [x] Increase WorkAdventure person/nameplate/status readability with modest marker/text scaling that stays non-overlapping against the current background (`frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx`, `src/styles.css`, tests/smoke)
- [x] Replace the current two-line WorkAdventure agent cards with the compact single-line `agent-name(taskCount)` nameplate format across work desks and lounge/rest markers, modestly raise readable nameplate font sizing, and extend tests/smoke to assert the exact label contract (`frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx`, `src/styles.css`, `src/components/WorkAdventureScene.test.tsx`, `scripts/prototypeBrowserSmoke.mjs`, docs)
- [x] Repair the Realtime page refresh path so automatic polls use a sane minimum cadence, preserve manual refresh, and stop remounting/rebuilding the React scene or visibly rebuilding the page on every live update (`clawobserver/static/app.js`, `tests/test_realtime_scene_logic.py`)
- [x] Extend automated guards to fail on rapid realtime refresh cadence, repeated scene remounts, excessive post-settle frames, or repeated steady-state background/Woka asset fetches; then run frontend tests/build/browser smoke and Python checks and record exact evidence here (`frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`, frontend tests, `tests/test_realtime_scene_logic.py`, `tests/test_server.py`, `tests/test_realtime_scene_logic.py`)

- 2026-05-13 WorkAdventure broader-workzone and refresh-stability repair: `specification/requirements.md`, `specification/design.md`, and `specification/tasks.md` now explicitly require broader work-area overlays on the approved custom background, slightly larger readable WorkAdventure name/status text, no visible realtime auto-refresh remount cadence, a sane minimum automatic refresh floor, and automated guards against rapid refresh / repeated steady-state asset fetches.
- 2026-05-13 renderer/layout repair details: `frontend/office-scene-prototype/src/data/workAdventurePreviewCatalog.ts` enlarged `desk-bay-west`, `desk-bay-east`, and `central-hotline` to cover the actual broader visible workspace. `frontend/office-scene-prototype/src/config/workAdventureLayout.ts` redistributed the 12 fixed work markers across those larger zones with small stagger offsets so the denser readable cards still avoid overlap. `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` now publishes the repaired readability/visibility contract (`data-scene-label-scale-hierarchy="single-line-count-nameplate-readable"`, `data-scene-nameplate-format="agent-name-taskcount-compact"`, `data-scene-peripheral-visibility="custom-background-broader-workzones-readable"`) and exposes per-agent DOM tags for smoke/test inspection.
- 2026-05-13 WorkAdventure readability repair details: `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` now renders both work-desk and lounge/rest badges with a shared compact single-line `agent-name(taskCount)` text contract instead of the old two-line `name` plus `work item(s)` card, except for truthful no-agent fallback copy. `frontend/office-scene-prototype/src/styles.css` modestly increased desk and lounge nameplate font sizing and padding while keeping lounge/rest correctness intact so the 7-person rest case remains inside the approved visible lounge with `nameplateOverlaps: []` in browser smoke.
- 2026-05-13 realtime refresh/performance repair details: `clawobserver/static/app.js` now keeps a stable realtime page shell after the first successful render instead of replacing `#page-root` and unmounting/remounting the React scene on every live poll. Normal realtime updates patch the existing KPI/table sections in place and rerender the embedded scene on the preserved `#realtime-r3f-scene-mount`. Automatic live refresh now enforces a frontend minimum floor of `30s`, while explicit recovery retries after failures still use the existing `3s` degraded retry cadence. Scene style/script loaders now fail soft in DOM-lite test environments instead of crashing on missing `document.head`.
- 2026-05-13 automated guard extension: `tests/test_realtime_scene_logic.py` now covers the new 30-second minimum auto-refresh floor and a second successful realtime render path that preserves the same mount node instead of replacing the live view. `frontend/office-scene-prototype/src/components/WorkAdventureScene.test.tsx` pins the updated broader-zone, compact-label, and readable-nameplate metadata. `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now asserts the new WorkAdventure readability metadata, the exact compact nameplate text contract, a `13px` minimum computed nameplate font size, rejects repeated steady-state background fetches, performs an in-page second realtime refresh, and ignores subpixel-only nameplate edge contact while still failing on real overlapping boxes.
- 2026-05-13 validation evidence for the focused WorkAdventure realtime repair:
  `python3 -m pytest tests/test_realtime_scene_logic.py` => passed (`17` tests).
  `node --check clawobserver/static/app.js` => passed.
  `npm --prefix frontend/office-scene-prototype test` => passed (`23` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh assets `prototype-DBCCoCDt.js`, `styles-BPXgrN9u.js`, updated `realtime-scene.css`, and updated `realtime-scene-embed.js`. Existing Vite runtime-resolution warning for `/assets/static_scene.jpg` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, and `failedRequests: 0` for both routes. Realtime smoke reported `labelHierarchy: "compact-count-nameplate"`, `labelScaleHierarchy: "single-line-count-nameplate-readable"`, `nameplateFormat: "agent-name-taskcount-compact"`, `nameplateMinFontPx: 13`, `smallestMeasuredNameplateFontPx: 13`, `invalidCompactNameplates: []`, `peripheralVisibility: "custom-background-broader-workzones-readable"`, `renderedAgentCount: 22`, `invalidWorkPlacements: []`, `invalidRestPlacements: []`, `restBoundingViolations: []`, `nameplateOverlaps: []`, `requestCounts["/api/live/overview"] = 2`, `requestCounts` for the custom background JPG and each Woka sprite remained `1`, and `postSettleAnimationFrames: 0`. Prototype smoke remained healthy with `postSettleAnimationFrames: 2`. Fresh screenshot artifacts: `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`, `tmp/realtime-avatar-preview-closeup.png`, `tmp/prototype-avatar-preview-closeup.png`, `tmp/prototype-workadventure-preview.png`.
  `python3 -m pytest tests/test_server.py tests/test_realtime_scene_logic.py` => passed (`27` tests).
  `python3 -m pytest` => passed (`59` tests).

- [x] Tighten the WorkAdventure lounge/rest area to the actual visible right/bottom lounge on the approved custom background, rework fixed + overflow rest markers so the current 7 idle/offline agents fit fully inside the visible lounge furniture/rug area with readable labels, and extend smoke to fail on full rest marker/nameplate bounds escaping the approved lounge (`frontend/office-scene-prototype/src/config/workAdventureLayout.ts`, `src/data/workAdventurePreviewCatalog.ts`, `src/lib/sceneState.ts`, `src/components/WorkAdventureScene.tsx`, `src/styles.css`, `src/components/WorkAdventureScene.test.tsx`, `src/lib/sceneState.test.ts`, `scripts/prototypeBrowserSmoke.mjs`)


- [x] Fix WorkAdventure realtime placement to render every `AgentVisualState` input, including overflow beyond fixed visible anchors, with no dropped live agents (`frontend/office-scene-prototype/src/lib/sceneState.ts`, `src/components/WorkAdventureScene.tsx`)
- [x] Enforce hard work/rest correctness so busy/error render only in work area and idle/offline render only in lounge/rest area; expand fixture coverage beyond the old 4-agent demo (`frontend/office-scene-prototype/src/data/mockAgents.ts`, tests, smoke fixture)
- [x] Extend smoke/visual assertions so rendered agent count equals input count, idle/offline in work area fails, busy/error in rest area fails, labels remain stable, and screenshots cover all visible agents (`frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`, tests, `tmp/` artifacts)
- 2026-05-12 lounge-bound correctness repair: the approved generated office background’s usable rest area is the right-side lounge room, not the earlier broad lower band. `frontend/office-scene-prototype/src/data/workAdventurePreviewCatalog.ts` and `frontend/office-scene-prototype/src/config/workAdventureLayout.ts` now tighten the declared `lounge-silent-zone` and add explicit `WORKADVENTURE_VISIBLE_LOUNGE_RECT` plus `WORKADVENTURE_REST_SAFE_RECT` contracts. Fixed rest markers moved onto the visible rug/sofa footprint, and overflow rest markers now use a compact second row inside that same room instead of drifting into the hallway / left lower floor.
- 2026-05-12 rest-marker layout repair details: `frontend/office-scene-prototype/src/lib/sceneState.ts` now seeds rest overflow from the stricter safe rectangle and offsets overflow rows below the first fixed row so the 7-agent rest case renders as a compact 4+3 lounge stack. `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` publishes the approved visible-lounge and rest-safe bounds as DOM metrics and tags each lounge marker with `data-wa-scene-nameplate-side`. `frontend/office-scene-prototype/src/styles.css` shrinks lounge badge/nameplate dimensions and flips second-row nameplates above their marker so every idle/offline agent remains fully inside the visible lounge while labels stay readable.
- 2026-05-12 smoke hardening for the visual blocker: `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now converts the approved lounge bounds from normalized map coordinates to viewport rects and fails if any rest marker box or lounge nameplate extends outside the approved visible lounge rectangle. The realtime smoke payload from this run reported `restOccupantCount: 7`, `renderedLoungeCount: 7`, `invalidRestPlacements: []`, `restBoundingViolations: []`, and `nameplateOverlaps: []`, which closes the specific idle/offline-outside-lounge regression the owner flagged.
- 2026-05-12 validation evidence for the lounge-bound repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`23` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-jY3p7cFc.js`, `styles-VaRdGmSf.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `/prototype` with `consoleErrors: 0`, `pageErrors: 0`, and `failedRequests: 0` for both routes. Realtime DOM metrics now report `visibleLoungeRectNormalized = { left: 0.67, top: 0.59, width: 0.28, height: 0.27 }`, `restSafeRectNormalized = { left: 0.701, top: 0.648, width: 0.209, height: 0.132 }`, `invalidRestPlacements: []`, `restBoundingViolations: []`, and `nameplateOverlaps: []`. Screenshot artifacts refreshed at `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`, `tmp/realtime-avatar-preview-closeup.png`, `tmp/prototype-avatar-preview-closeup.png`, and `tmp/prototype-workadventure-preview.png`.
  `python3 -m pytest tests/test_server.py tests/test_realtime_scene_logic.py` => passed (`25` tests).
  `python3 -m pytest` => passed (`57` tests).

- [x] Import the approved generated office map background with provenance and use it as the primary WorkAdventure realtime visual background without deleting official WorkAdventure resources or legacy 3D assets (`frontend/office-scene-prototype/public/office-assets`, attribution/provenance docs)
- [x] Re-map WorkAdventure anchors so working/busy agents appear in the work area and idle/non-working agents appear in the rest/lounge area; remove disruptive desk watermark labels and keep agent name + work count labels stable (`frontend/office-scene-prototype/src/components`, `src/data`, `src/styles.css`)
- [x] Move selected-agent detail rendering below the visualization as a horizontal strip and verify it does not push or misalign the map (`frontend/office-scene-prototype/src/components`, `src/styles.css`)
- [x] Extend smoke/tests/screenshots to catch custom background load failures, work/rest misplacement, label overlap/clipping, incorrect occlusion, and retained `/prototype` regressions (`frontend/office-scene-prototype/scripts`, tests, `tmp/` artifacts, `specification/tasks.md`)

- [x] Embed WorkAdventure-style tile-map renderer as the primary Realtime scene while preserving the existing R3F/3D scene as a retained alternate style (`frontend/office-scene-prototype/src/components`, `src/realtimeSceneEmbed.tsx`, static bundle integration)
- [x] Add audited official WorkAdventure avatar/Woka resource subset with provenance/license metadata and render live-data-derived people/person markers instead of generic placeholders (`frontend/office-scene-prototype/public/office-assets/workadventure-*`, `src/data`, attribution docs)
- [x] Refine WorkAdventure visual style for current product use: cleaner palette, single-person desk areas, readable hanging/nameplate labels, status badges, and interaction-zone legibility (`frontend/office-scene-prototype/src/components`, `src/styles.css`)
- [x] Extend tests/smoke/screenshots to validate embedded Realtime WorkAdventure mode, retained 3D/prototype route, live-data labels, avatar resources, zero page/console errors, and visual evidence (`frontend/office-scene-prototype/scripts`, tests, `tmp/` artifacts, `specification/tasks.md`)

- [x] WorkAdventure-direction demo: add/update spec-backed preview assets and provenance for official WorkAdventure map-starter-kit resources without replacing the production R3F scene (`frontend/office-scene-prototype/public`, docs/license/provenance files)
- [x] WorkAdventure-direction demo: implement a lightweight browser-rendered tile-map virtual-office sample with office map, walkable/collision concept, interaction zones, and reusable avatar/sprite placeholders (`frontend/office-scene-prototype/src`, `clawobserver/static/prototype`)
- [x] WorkAdventure-direction demo: extend build/browser smoke and screenshot evidence so visual validation covers the new sample in addition to existing realtime/prototype checks (`frontend/office-scene-prototype/scripts`, `tmp/` artifacts, `specification/tasks.md`)
- [x] Preview-only pass: replace the visible primitive/blocky avatar preview with the approved repo-local Poly Pizza `Hyper Casual Character` CC BY 3.0 GLB, seat 2-4 unmistakably human-looking preview instances coherently at desks, preserve readable labels, extend attribution/contracts, and regenerate screenshot evidence without claiming a final avatar system (`frontend/office-scene-prototype/public/office-assets/poly-pizza-hyper-casual-preview`, `frontend/office-scene-prototype/src/components`, `frontend/office-scene-prototype/src/data`, docs, smoke artifacts)
- [x] Preview-only pass: import a tiny repo-local Kenney Blocky Characters GLB subset with explicit provenance/license metadata, seat 3-5 placeholders at selected desks in the shared office scene without changing the live scene contract, and regenerate build/smoke evidence as non-final preview output (`frontend/office-scene-prototype/public/office-assets/kenney-blocky-preview`, `frontend/office-scene-prototype/src/components`, `frontend/office-scene-prototype/src/data`, docs)
- [x] Fix the workstation visual blocker in the shared R3F office scene so desks read as wide camera-facing workstations, monitors are the focal object with visible keyboard/mouse detail, labels shrink into metadata tags above the monitor plane, and the palette stays in the deep navy / steel / cyan command-center range without breaking provenance/offline/performance contracts (`frontend/office-scene-prototype/src/components/AgentDesk.tsx`, `frontend/office-scene-prototype/src/config/visualMapping.ts`, `frontend/office-scene-prototype/src/config/workstationSlots.ts`)
- [x] Extend scene metadata/tests/smoke coverage for the repaired workstation hierarchy so browser validation can fail on lost camera-facing desk orientation, missing integrated monitor detail, or oversized label hierarchy regressions (`frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`, `frontend/office-scene-prototype/src/lib/sceneState.test.ts`, `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`)
- [x] Re-run frontend tests, build, browser smoke, and full `pytest`, then record final screenshot evidence for the workstation-hierarchy repair in this task log
- [x] Repair the temporary avatar preview composition so monitor fronts no longer read reversed relative to the seated avatar, and switch the visible preview away from square-head Blocky presentation to a clearly temporary rounded primitive avatar while preserving Kenney provenance/license docs for the preview seam (`frontend/office-scene-prototype/src/components/AgentDesk.tsx`, `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`, `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`, docs)

- [x] Update root planning docs first for the visual-refinement pass so the shared scene contract explicitly requires the deep navy / steel / cobalt / cyan command-center palette, camera-facing desk fronts and monitor screens, and lightweight enhanced monitor styling before implementation (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Refine the existing shared R3F office scene without structural churn: recolor shell/furniture/props toward the darker tech-command-center palette, rotate every fixed workstation so desk fronts and screens face the camera consistently, and strengthen monitor-screen readability while preserving the current label seam, layout, provenance, and performance guards (`frontend/office-scene-prototype/src/components/*.tsx`, `frontend/office-scene-prototype/src/config/*.ts`)
- [x] Extend scene metadata and smoke/test assertions where practical so the palette/orientation/monitor-style contract is explicitly checked alongside the existing front-label-lane, opacity, provenance, screenshot-richness, and frameloop guarantees (`frontend/office-scene-prototype/src/lib/sceneState.test.ts`, `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`)
- [x] Re-run frontend tests, build, browser smoke for `/` and `/prototype`, and full `pytest`; record concrete validation evidence for the visual-refinement pass in this task log

- [x] Update root planning docs to capture the Quaternius-inspired toy-office style goal, deterministic provenance constraints, and required validation evidence before implementation (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Re-style the shared R3F office scene toward a chunkier, warmer, toy-like office using safe repo-local assets and primitive augmentation while preserving the current `AgentVisualState` contract, full-width layout, opaque desk structures, clear overhead sightline, primitive fallback seam, and `frameloop="demand"` performance guard (`frontend/office-scene-prototype/src/components/*.tsx`, `frontend/office-scene-prototype/src/config/*.ts`)
- [x] Extend smoke/metadata coverage where practical so the stylized visual mode remains asserted alongside existing provenance/performance/layout guarantees (`frontend/office-scene-prototype/src/lib/sceneState.test.ts`, `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`)
- [x] Re-run frontend tests, build, browser smoke for `/` and `/prototype`, and relevant pytest coverage; record concrete evidence in `specification/tasks.md`

- [x] Compact the 12-desk workstation layout and camera framing so the scene fills the Realtime page horizontally without excessive inter-desk whitespace (`frontend/office-scene-prototype/src/config/workstationSlots.ts`, `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`)
- [x] Move `SceneStatusSummary` / scene summary content above the 3D canvas and make the production scene the dominant full-width region instead of shrinking it beside a right stack (`frontend/office-scene-prototype/src/components/EmbeddedRealtimeScenePanel.tsx`, `frontend/office-scene-prototype/src/styles.css`)
- [x] Increase desk/monitor/label/status/task-stack scale and legibility while preserving the 12-desk live-state mapping (`frontend/office-scene-prototype/src/components/AgentDesk.tsx`, related scene components)
- [x] Adopt a curated repo-local Kenney `Furniture Kit` subset for desks/chairs/monitor gear/plants/lounge tables/bookcases with explicit license and provenance docs, deterministic local loading, and primitive fallbacks (`frontend/office-scene-prototype/public/office-assets/kenney`, `frontend/office-scene-prototype/src/components`, docs)
- [x] Extend browser smoke/visual assertions to catch overly wide spacing, undersized desks, missing summary-above-canvas layout, and scene fit regressions for production `/` (`frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`)

- [x] Update root planning docs so production Realtime `/` explicitly uses the React Three Fiber office scene instead of a prototype-only route, including required browser validation evidence for `/` (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Migrate the R3F office scene into the production Realtime page by replacing the current static-image center scene area while preserving existing history/runtime/dashboard behavior (`clawobserver/static/app.js`, `clawobserver/static/index.html`, built static assets)
- [x] Refactor the prototype scene into a reusable embeddable production module with stable Python-served asset paths, while retaining `/prototype` as an optional dev/demo route (`frontend/office-scene-prototype`, `clawobserver/static/prototype/`)
- [x] Wire `/api/live/overview` state into the production scene so live updates drive desk status, monitor glow, status lamps, file/task stacks, desk labels, status board, and selected detail view truthfully for busy/idle/error/offline/waiting states
- [x] Improve the office scene visual design to better match `clawobserver/static/assets/static_scene.jpg`, with warmer friendlier skeuomorphic work/lounge areas, clearer hierarchy, and lightweight state cues
- [x] Extend frontend/browser validation to the real `/` Realtime page with deterministic live fixture coverage, zero console/page/request errors, screenshot artifact paths under `tmp/`, and visible assertions for 12 desks, lounge, and status board
- [x] Fix the shared browser smoke so external `/` and `/prototype` URLs select the correct validator, optional `PROTOTYPE_SMOKE_MODE=realtime|prototype|both` is supported, and production `/` no longer fails on a brittle bottom-band brightness assumption
- [x] Update root planning docs as implementation prerequisites for the minimal Three.js + React Three Fiber Realtime scene direction, including mandatory MVP constraints: exactly 12 fixed desks plus `clawobserver/static/assets/static_scene.jpg` work-area and reserved lounge-area reference (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)
- [x] Implementation run: add stable renderer-facing data contract `AgentVisualState` plus a fixed 12-slot workstation config constant (`frontend/office-scene-prototype/src/agentVisualState.ts`, `frontend/office-scene-prototype/src/config/workstationSlots.ts`)
- [x] Implementation run: add adapter boundary mapping live ClawObserver/OpenClaw runtime data to `AgentVisualState[]` without renderer coupling to OpenClaw internals (`frontend/office-scene-prototype/src/lib/agentVisualAdapter.ts`)
- [x] Implementation run: build minimal embeddable R3F scene components (`frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`, `frontend/office-scene-prototype/src/components/OfficeShell.tsx`, `frontend/office-scene-prototype/src/components/DeskGrid.tsx`, `frontend/office-scene-prototype/src/components/AgentDesk.tsx`, `frontend/office-scene-prototype/src/components/GlobalStatusBoard.tsx`) and HTML `AgentDetailPanel`
- [x] Implementation run: enforce required status/task visual mapping (`idle` green low-brightness, `busy` blue monitor highlight plus capped stack, `error` red alert/lamp, `offline` grey desk/monitor off, capped task-count object creation)
- [x] Implementation run: add hover highlight/name and click selection/detail interactions
- [x] Implementation run: add independent mock/demo data path when live OpenClaw data is unavailable
- [x] Implementation run: add focused tests for adapter/contract mapping, fixed 12 desks, taskCount cap, status visual config mapping, interaction behavior, and scene smoke/build checks (`frontend/office-scene-prototype/src/lib/*.test.ts`, `frontend/office-scene-prototype/src/components/AgentDetailPanel.test.tsx`, `tests/test_server.py`, `python3 -m compileall clawobserver`, `npm --prefix frontend/office-scene-prototype run build`)
- [x] Implementation run: integrate into existing dashboard realtime page when safe, otherwise provide repo-local prototype route/page without destabilizing current Python dashboard

- [x] Sync root docs so the measured Realtime scene-layout contract, percentage-normalized coordinate loading, active workstation placement, idle lounge-only placement, and placeholder-capable empty desks are documented in the existing root docs (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`, `README.md`)
- [x] Fix Realtime scene config/state alignment so hanging tags use measured `static_scene.jpg` geometry and idle agents render only in lounge while their workstation anchors remain unassigned (`clawobserver/static/reference-scene-layout.json`, `clawobserver/static/app.js`, `tests/test_realtime_scene_logic.py`)
- [x] Restore the real OpenClaw data chain so Realtime and archive capture stop writing empty/waiting-only snapshots when bounded local session stores are available (`scripts/openclaw_runtime_adapter.py`, `clawobserver/runtime.py`, `clawobserver/app.py`).
- [x] Prove the true chain split: frontend, archive schema, timer, and SQLite insertion were healthy; the empty-today failure came from the runtime adapter's hanging all-agents/gateway collection path zeroing the upstream snapshot before archive capture (`specification/tasks.md`, live validation).
- [x] Reconnect Phase A historical restoration and Phase B live OpenClaw data collection with regression tests covering local-store-primary sessions plus degraded gateway/all-agents failures (`tests/test_openclaw_runtime_adapter.py`, `tests/test_runtime.py`, `tests/test_server.py`, `tests/test_archive.py`).
- [x] Reproduce and document the actual Realtime refresh failure chain, including the backend `/api/live/overview` waiting-shape crash (`scripts/openclaw_runtime_adapter.py`, tests, spec docs).
- [x] Explain and verify why retries remained ineffective before the fix: the adapter hit the same deterministic nested-shape exception on every request before any first successful payload existed.
- [x] Fix the core cause in code by normalizing recoverable gateway/session waiting shapes into a valid live payload instead of throwing (`scripts/openclaw_runtime_adapter.py`).
- [x] Add/update tests that cover the root cause and repaired first-screen refresh path, not just retry behavior (`tests/test_openclaw_runtime_adapter.py`, `tests/test_realtime_scene_logic.py`).
- [x] Reproduce the still-open live failure in the current repo state: `clawobserver/runtime.py` crashes on `sessions: null`, which makes `/api/live/overview` return 503 and drives first-load `Load failed`.
- [x] Fix `clawobserver/runtime.py` normalization so null/malformed top-level runtime containers degrade to `capture_status: "waiting"` instead of throwing.
- [x] Add regression coverage for the repaired runtime/server path (`tests/test_runtime.py`, `tests/test_server.py`).
- [x] Extend the runtime/server guard to malformed top-level live payloads and invalid `captured_at`, which still bypassed waiting-state normalization and could keep `/api/live/overview` on the 503 path (`clawobserver/runtime.py`, `tests/test_runtime.py`, `tests/test_server.py`).
- [x] Add bounded/fail-soft OpenClaw CLI command handling in `scripts/openclaw_runtime_adapter.py`.
- [x] Add bounded/fail-soft runtime command handling in `clawobserver/runtime.py`.
- [x] Add controlled live API error response handling in `clawobserver/server.py`.
- [x] Add frontend realtime fetch timeout handling in `clawobserver/static/app.js`.
- [x] Add tests for adapter command failure, runtime command timeout/failure, server controlled response, and frontend hanging fetch timeout.
- [x] Fix the real 8420 timeout-contract mismatch: backend runtime-command fail-soft now returns before the frontend's 4s live abort so first-load waiting payloads can render.
- [x] Add regression coverage for the 8420 timeout contract and a delayed first waiting payload reaching the Realtime scene.
- [x] Re-run unit, syntax, compile, HTTP smoke, and production-like runtime failure validation.
- [x] Run local unit/syntax checks and browser/DOM-level validation for Realtime refresh, first load, continued updates, and short recovery.
- [x] Repair Realtime scene model/tag/status logic for mixed active/idle agents (`clawobserver/static/app.js`, `tests/test_realtime_scene_logic.py`)
- [x] Validate locally with syntax/unit/browser-or-DOM smoke checks (`clawobserver/static/app.js`, `tests`)
- [x] Run independent Kimi acceptance review (`kimi-cli`) — **accepted 2026-05-08**
- [x] Record concise result and key validation in workspace memory (`/mnt/data/workspace-se-codex/memory/2026-05-08.md`)
- [x] Fix Realtime hanging-tag placeholder rendering so waiting/unassigned desks still show explicit tag label/count instead of dropping tag DOM (`clawobserver/static/app.js`, `tests/test_realtime_scene_logic.py`)
- [x] Validate hanging-tag regression with targeted DOM-string tests plus JavaScript syntax check (`tests/test_realtime_scene_logic.py`, `node --check clawobserver/static/app.js`)
- [x] Phase A: restore historically normal non-empty Realtime behavior by preventing all-agents CLI failure from collapsing the page into an empty/waiting shell (`scripts/openclaw_runtime_adapter.py`, `tests/test_openclaw_runtime_adapter.py`, `tests/test_realtime_scene_logic.py`)
- [x] Phase B: connect truthful OpenClaw realtime session data from bounded local runtime sources by rebuilding all-agent session payloads from `~/.openclaw/agents/*/sessions/sessions.json` and merging fast CLI data when available (`scripts/openclaw_runtime_adapter.py`, `tests/test_openclaw_runtime_adapter.py`)
- [x] Validate the repaired real runtime path end-to-end with the production adapter command and confirm non-empty `openclaw-cli-runtime` payloads despite degraded gateway/all-agent commands
- [x] Reproduce the `/prototype` React Three Fiber black-screen in a real browser, identify the exact dashed-prop misuse in `AgentDesk`, and remove the crash with the smallest safe scene change (`frontend/office-scene-prototype/src/components/AgentDesk.tsx`).
- [x] Add committed browser-runtime smoke coverage for `/prototype` that starts a local ClawObserver server, fails on console/page/request errors, and asserts rendered scene/canvas presence (`frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs`, `frontend/office-scene-prototype/package.json`).

## Done
- [x] Confirm focused scope: only ClawObserver Realtime hanging-tag display and agent status display
- [x] Update SPEC docs for the focused 2026-05-07 repair (`specification/requirements.md`, `specification/design.md`, `specification/tasks.md`)

## Notes
- **Independent verification on 2026-05-13 (Kimi Code CLI)**: independently verified the 2026-05-13 WorkAdventure realtime repair focused on broader work-area overlays, larger readable markers, and realtime refresh stability.
  - Changed files review: `frontend/office-scene-prototype/src/data/workAdventurePreviewCatalog.ts` defines broader work zones (`desk-bay-west` 0.38×0.33, `desk-bay-east` 0.27×0.33, `central-hotline` 0.60×0.24) covering the actual visible workspace. `frontend/office-scene-prototype/src/config/workAdventureLayout.ts` redistributes 12 fixed desk markers across those larger zones with stagger offsets, keeps `WORKADVENTURE_VISIBLE_LOUNGE_RECT` and `WORKADVENTURE_REST_SAFE_RECT` contracts tight, and seats 4 fixed lounge markers on the visible rug/sofa footprint with overflow below. `frontend/office-scene-prototype/src/styles.css` modestly enlarges work desk markers (96px), nameplates (86px, 0.74rem), and status badges (28px) while retuning lounge badges smaller (78px marker, 72px nameplate, 0.61rem) so the 7-person rest case fits inside the visible lounge. `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` publishes the repaired readability contract (`data-scene-label-scale-hierarchy="single-occupant-desk-badge-readable"`, `data-scene-peripheral-visibility="custom-background-broader-workzones-readable"`) and exposes per-agent DOM tags for inspection. `clawobserver/static/app.js` introduces `MIN_AUTO_LIVE_REFRESH_SECONDS = 30`, `scheduleLiveRefresh(..., { respectMinimum })`, and `ensureRealtimePageShell` so automatic polls patch the existing shell instead of replacing `#page-root` and unmounting the scene; recovery retries preserve the existing 3s cadence via `respectMinimum: false`. `frontend/office-scene-prototype/src/realtimeSceneEmbed.tsx` caches the React root on the mount node so re-renders update props in place without remount. `tests/test_realtime_scene_logic.py` adds `test_realtime_auto_refresh_enforces_sane_minimum_floor` (asserts 30s floor) and `test_realtime_second_success_keeps_scene_mount_and_updates_in_place` (asserts same mount node, one creation, updated payload). `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` asserts zero repeated Kenney/WorkAdventure asset requests, zero post-settle frames exceeding `MAX_IDLE_ANIMATION_FRAMES = 8`, and performs an in-page `refreshPage` probe that verifies the mount node identity is preserved.
  - Regression checks with evidence:
    - **lounge/rest bounds**: realtime smoke reported `invalidRestPlacements: []` and `restBoundingViolations: []`; the 7-agent rest case renders fully inside the approved visible lounge.
    - **nameplate overlap**: realtime smoke reported `nameplateOverlaps: []`; overlap threshold ignores subpixel edge contact (`overlapWidth > 1 && overlapHeight > 1`).
    - **repeated asset fetches**: smoke passed `repeatedAssetRequests.length === 0` and `repeatedWorkAdventureRequests.length === 0`; `ensureRealtimeSceneScript` and `ensureRealtimeSceneStyles` are singleton-guarded in `app.js`, and the embed script caches its React root.
    - **post-settle animation frames**: realtime route `postSettleAnimationFrames: 0`; prototype R3F route `postSettleAnimationFrames: 3`, both within the `MAX_IDLE_ANIMATION_FRAMES = 8` guard.
    - **refresh interval floor**: `MIN_AUTO_LIVE_REFRESH_SECONDS = 30` in `app.js`; Python DOM-VM test `test_realtime_auto_refresh_enforces_sane_minimum_floor` asserts `scheduledDelay === 30000`.
    - **manual refresh preservation**: recovery retries use `scheduleLiveRefresh(..., { respectMinimum: false })` preserving the 3s degraded cadence; smoke explicitly calls `refreshPage({ showLoading: false })` and asserts `firstMount === document.getElementById("realtime-r3f-scene-mount")`.
  - Commands run and results:
    - `python3 -m pytest` => passed (`59` tests).
    - `node --check clawobserver/static/app.js` => passed.
    - `npm --prefix frontend/office-scene-prototype test` => passed (`23` tests).
    - `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh assets `prototype-wjBds5Qc.js`, `styles-KGKM9WNY.js`, updated `realtime-scene.css`, and updated `realtime-scene-embed.js`.
    - `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Realtime smoke reported `labelScaleHierarchy: "single-occupant-desk-badge-readable"`, `peripheralVisibility: "custom-background-broader-workzones-readable"`, `renderedAgentCount: 22`, `inputAgentCount: 22`, `invalidWorkPlacements: []`, `invalidRestPlacements: []`, `restBoundingViolations: []`, `nameplateOverlaps: []`, `postSettleAnimationFrames: 0`, and `detailStripLayout: "below-map-stable-horizontal"`. Prototype smoke reported `postSettleAnimationFrames: 3` and preserved all retained 3D contracts (`frameloop: "demand"`, `performanceMode: "idle-on-demand"`).
  - Verdict: **PASS**. No regressions detected in the six targeted areas. All automated guards, file-level contracts, and visual assertions are satisfied by the current working tree.
- **Independent verification on 2026-05-13 (Kimi Code CLI) — compact WorkAdventure nameplate pass**: independently verified the focused nameplate change from the old two-line `name` + `work item(s)` card to the compact single-line `agent-name(taskCount)` format across both work-desk and lounge/rest markers, with modestly increased readable font sizing.
  - Nameplate format review: `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` `buildAgentNameplateLabel(agent)` returns `${agent.name}(${Math.max(0, Math.trunc(agent.taskCount))})`, producing exact single-line text such as `se-codex(1)`. Both work desk (`<span className="wa-scene-desk-nameplate">`) and lounge/rest (`<span className="wa-scene-lounge-nameplate">`) markers consume this shared function. The component root publishes `data-scene-nameplate-format="agent-name-taskcount-compact"` and `data-scene-nameplate-min-font-px="13"` as explicit DOM contracts.
  - Font sizing review: `frontend/office-scene-prototype/src/styles.css` sets `.wa-scene-desk-nameplate { font-size: 14px; }` and `.wa-scene-lounge-nameplate { font-size: 13px; }`, both meeting or exceeding the published `13px` minimum. The prior two-line card layout (which required smaller split-line text) has been replaced by the single-line badge, so the modest increase is readable without expanding the bounding box enough to cause overlap.
  - Zero "work item" residue: `grep -r "work item" frontend/office-scene-prototype/src/` returned zero matches. The only remaining "work item" occurrence in the frontend tree is in `scripts/prototypeBrowserSmoke.mjs`, where it is explicitly used as a negative assertion (`nameplate.text.includes("work item")` must be false) to guard against regression.
  - Smoke contract verification: `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` enforces `/^[^()\s][^()]*\(\d+\)$/` for every combined work + lounge nameplate, asserts `smallestMeasuredNameplateFontPx >= 13`, and rejects any nameplate containing "work item". The latest browser smoke run reported `invalidCompactNameplates: []`, `smallestMeasuredNameplateFontPx: 13`, and `nameplateOverlaps: []`.
  - Regression checks with evidence:
    - **nameplate overlap**: realtime smoke reported `nameplateOverlaps: []` with subpixel-edge contact ignored (`overlapWidth > 1 && overlapHeight > 1`).
    - **rest bounds**: realtime smoke reported `restBoundingViolations: []` and `invalidRestPlacements: []`; the 7-agent rest case remains fully inside the approved visible lounge.
    - **performance refresh**: realtime smoke reported `postSettleAnimationFrames: 0`, confirming no refresh-induced animation or remount regression.
  - Commands run and results:
    - `python3 -m pytest` => passed (`59` tests).
    - `node --check clawobserver/static/app.js` => passed.
    - `npm --prefix frontend/office-scene-prototype test` => passed (`23` tests).
    - `npm --prefix frontend/office-scene-prototype run build` => passed; emitted `prototype-DBCCoCDt.js`, `styles-BPXgrN9u.js`, updated `realtime-scene.css`, and updated `realtime-scene-embed.js`. Existing Vite runtime-resolution warning for `/assets/static_scene.jpg` remained non-fatal and unchanged.
    - `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Realtime smoke reported `nameplateFormat: "agent-name-taskcount-compact"`, `nameplateMinFontPx: 13`, `smallestMeasuredNameplateFontPx: 13`, `invalidCompactNameplates: []`, `nameplateOverlaps: []`, `restBoundingViolations: []`, `postSettleAnimationFrames: 0`, `renderedAgentCount: 22`, `invalidWorkPlacements: []`, and `invalidRestPlacements: []`. Prototype smoke reported `postSettleAnimationFrames: 2` and preserved all retained 3D contracts (`frameloop: "demand"`, `performanceMode: "idle-on-demand"`).
  - Verdict: **PASS**. The compact single-line `agent-name(taskCount)` nameplate contract is correctly implemented for both work and lounge markers, the minimum `13px` font size is enforced and readable, no "work item" text remains in rendered nameplates, and no overlap, rest-bound, or performance-refresh regressions were introduced.
- 2026-05-12 approved custom WorkAdventure realtime background pass completed: imported the approved office JPG repo-locally at `frontend/office-scene-prototype/public/office-assets/workadventure-custom-background/generated-office-2026-05-12-1420.jpg` with pinned provenance in `frontend/office-scene-prototype/public/office-assets/workadventure-custom-background/provenance.json` (`1264x848`, `609333` bytes, sha256 `ff3fb57e3365587ed4c0215f954901b86cc9298d9adb018bea97afbdad7b120c`). `THIRD_PARTY_ATTRIBUTIONS.md` now documents the approved generated-image source path and usage scope, and the build copies the background into `clawobserver/static/prototype/office-assets/workadventure-custom-background/` beside the retained official WorkAdventure and legacy 3D assets.
- 2026-05-12 WorkAdventure placement/layout stability pass completed: `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx`, `src/lib/sceneState.ts`, `src/data/workAdventurePreviewCatalog.ts`, and `src/styles.css` now route `busy|error` agents into explicit work-area anchors and `idle|offline` agents into lounge/rest anchors (`data-scene-placement-contract="busy-work-idle-rest"`), remove disruptive empty-desk watermark/nameplate clutter, keep occupied labels to agent name plus current work-count text, and move the selected-agent detail panel below the visualization as a stable horizontal strip (`data-detail-strip-layout="below-map-stable-horizontal"`). The retained `/prototype` legacy R3F route and the official WorkAdventure/Woka asset paths remain intact.
- 2026-05-12 validation evidence for the custom WorkAdventure background and stability pass:
  `npm --prefix frontend/office-scene-prototype test` => passed (`20` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `python3 -m compileall clawobserver` => passed.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-CiQp57zP.js`, `styles-Bm93eXIV.js`, updated `realtime-scene-embed.js`, updated `realtime-scene.css`, and copied `workadventure-custom-background/` alongside `workadventure-preview/`, `workadventure-woka-subset/`, and the retained Poly Pizza preview assets.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Realtime metadata asserted the new custom background and geometry contract: `backgroundMode: "custom-generated-office-background"`, `backgroundSource: "approved-user-generated-office-2026-05-12"`, `backgroundWidth/Height: 1264x848`, `backgroundNaturalWidth/Height: 1264x848`, `placementContract: "busy-work-idle-rest"`, `busyWorkCount: 2`, `restOccupantCount: 2`, `invalidWorkPlacements: []`, `invalidRestPlacements: []`, `detailStripLayout: "below-map-stable-horizontal"`, and `nameplateOverlaps: []`. Prototype metadata simultaneously preserved the retained legacy 3D route and preview contracts (`frameloop: "demand"`, `performanceMode: "idle-on-demand"`, `waPreviewMode: "workadventure-office-map-preview"`).
  Fresh screenshot artifacts from the smoke harness: `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`, `tmp/prototype-avatar-preview-closeup.png`, `tmp/realtime-avatar-preview-closeup.png`, and `tmp/prototype-workadventure-preview.png`. Current screenshot metrics after the pass: realtime `fileBytes: 1416956`, `brightPixels: 595608`, `darkPixels: 128008`, `midVariancePixels: 408317`, `uniqueBuckets: 4096`, `lumaSpread: 255.00`; prototype `fileBytes: 195309`, `brightPixels: 429846`, `darkPixels: 73149`, `midVariancePixels: 345314`, `uniqueBuckets: 4096`, `lumaSpread: 255.00`.
  `python3 -m pytest` => passed (`57` tests).

- 2026-05-12 WorkAdventure-direction preview demo completed without replacing the production R3F path. Imported a repo-local official WorkAdventure `map-starter-kit` office subset under `frontend/office-scene-prototype/public/office-assets/workadventure-preview/` with pinned provenance at `provenance.json`, bundled upstream `LICENSE.assets` and `LICENSE.map`, and attribution updates in `THIRD_PARTY_ATTRIBUTIONS.md`. The prototype-only React surface now renders a browser-visible 2D tile-map preview panel alongside the existing scene using the official `office.png` thumbnail plus explicit walkable/collision overlays, four reusable avatar placeholders, and four office/interaction highlight zones while leaving `/` and the live `AgentVisualState` contract unchanged.
- 2026-05-12 validation evidence for the WorkAdventure-direction preview demo:
  `npm --prefix frontend/office-scene-prototype test` => passed (`16` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-pVIfbxuc.js`, `realtime-scene.css`, updated `realtime-scene-embed.js`, and copied `workadventure-preview/` into the Python-served static tree beside the existing Poly Pizza preview assets.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype metadata now reported `waPreviewMode: "workadventure-office-map-preview"`, `waPreviewSource: "official-workadventure-map-starter-kit"`, `waPreviewLicenseScope: "workadventure-map-only"`, `waPreviewMapWidth/Height: 31x21`, `waPreviewVisibleTileLayerCount: 11`, `waPreviewWalkableTileCount: 366`, `waPreviewCollisionTileCount: 285`, `waPreviewZoneCount: 7`, `waPreviewZoneOverlayCount: 4`, `waPreviewAvatarCount: 4`, `waPreviewTilesetCount: 10`, and the expected repo-local map/license/provenance paths. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`, `tmp/prototype-avatar-preview-closeup.png`, `tmp/realtime-avatar-preview-closeup.png`, `tmp/prototype-workadventure-preview.png`.
  Browser-smoke screenshot metrics remained healthy after adding the 2D panel: prototype `fileBytes: 195249`, `brightPixels: 413731`, `darkPixels: 84010`, `midVariancePixels: 329429`, `uniqueBuckets: 4096`, `lumaSpread: 255.00`; realtime `fileBytes: 187694`, `brightPixels: 421869`, `darkPixels: 64888`, `midVariancePixels: 341140`, `uniqueBuckets: 4096`, `lumaSpread: 254.93`. The prototype preview-specific closeup artifact `tmp/prototype-workadventure-preview.png` was written at `260426` bytes.
  `python3 -m pytest` => passed (`57` tests).

- **Independent verification on 2026-05-12 (Kimi Code CLI)**: full independent verification of the primary WorkAdventure realtime renderer implementation.
  - Changed files review: `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` (new primary 2D tile-map renderer with 12 fixed desks, official Woka avatars, status badges, task stacks, lounge markers, interaction zones), `SceneRenderer.tsx` (mode switcher `workadventure`|`legacy-3d`), `EmbeddedRealtimeScenePanel.tsx` (production `/` hardcodes `mode="workadventure"`), `App.tsx` (`/prototype` hardcodes `rendererMode="legacy-3d"` with `showWorkAdventurePreview=true`). `agentVisualState.ts` shows zero diff; contract preserved intact.
  - Legacy R3F retention: `/prototype` continues to render `AgentOfficeScene` via `SceneRenderer` in `legacy-3d` mode. All prior Kenney asset pipeline, primitive fallback seam, `frameloop="demand"`, and performance guards remain in the retained 3D path.
  - Asset provenance/license review:
    - `workadventure-preview/` provenance pins upstream `workadventure/map-starter-kit` at commit `7ce4a7c87380094357bd608805313dc2c737007a` with sha256 checksums for all 14 files, `previewOnly: true`, `usageScope: "workadventure-map-only"`.
    - `workadventure-woka-subset/` provenance pins upstream `workadventure/workadventure` at commit `b94fa79f8313eb90e606847669044cd0631029a8` with sha256 checksums for all 6 files, `usageScope: "workadventure-runtime-demo-only"`, bundled `WORKADVENTURE-play-LICENSE.txt` and `pipoya-about.txt`.
    - **Blocker found and fixed**: `THIRD_PARTY_ATTRIBUTIONS.md` was missing the WorkAdventure Woka runtime subset entry. Added attribution covering upstream repo, commit, sprite names, license scope, and local paths. The preview map subset was already documented; the Poly Pizza preview avatar was already documented.
  - Build/static output: `npm run build` succeeded; emitted `prototype-BYN3vpZE.js`, `styles-BeU9k7NW.js`, updated `realtime-scene-embed.js`, updated `realtime-scene.css`, and copied `workadventure-preview/` and `workadventure-woka-subset/` into `clawobserver/static/prototype/office-assets/`.
  - Browser smoke coverage: `PROTOTYPE_SMOKE_PORT=8451 npm run smoke:browser` passed on isolated fixture-backed server with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both `/` and `/prototype`. Realtime smoke asserted `sceneKind: "workadventure-primary"`, `rendererMode: "workadventure"`, `assetStrategy: "workadventure-tilemap-runtime-subset"`, `wokaMode: "workadventure-runtime-woka-subset"`, `wokaSubsetCount: 4`, `renderedDeskCount: 12`, `renderedZoneCount: 4`, `renderedWokaCount: 4`, `renderedLoungeCount: 3`, license/provenance path assertions, and screenshot richness (`brightPixels: 626492`, `midVariancePixels: 462821`, `uniqueBuckets: 4096`). Prototype smoke asserted retained 3D metadata including `frameloop: "demand"`, `performanceMode: "idle-on-demand"`, and `waPreviewMode: "workadventure-office-map-preview"`.
  - Verification commands run:
    - `npm --prefix frontend/office-scene-prototype test` => `17 passed`
    - `npm --prefix frontend/office-scene-prototype run build` => success
    - `python3 -m compileall clawobserver` => success
    - `python3 -m pytest` => `57 passed`
    - `node --check clawobserver/static/app.js` => success
    - `PROTOTYPE_SMOKE_PORT=8451 npm --prefix frontend/office-scene-prototype run smoke:browser` => success (exit 0)
  - Verdict: **PASS** after fixing the missing Woka attribution in `THIRD_PARTY_ATTRIBUTIONS.md`. No remaining blockers.
- 2026-05-12 primary WorkAdventure renderer pass completed: production `/` now mounts a live-data-driven embedded WorkAdventure-style office surface through the existing `realtimeSceneEmbed.tsx` seam, while `/prototype` retains the previous React Three Fiber office scene as the current legacy 3D route. The new primary scene lives in `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` behind `SceneRenderer.tsx`, renders twelve fixed desk markers from `AgentVisualState[]`, uses readable hanging desk headers plus single-person nameplates/status badges/task stacks, and preserves the existing selected-detail/summary flow.
- 2026-05-12 official WorkAdventure avatar/Woka subset pass completed: imported a minimal repo-local official runtime subset under `frontend/office-scene-prototype/public/office-assets/workadventure-woka-subset/` with pinned provenance in `provenance.json`, bundled upstream `play/LICENSE.txt` as `WORKADVENTURE-play-LICENSE.txt`, and copied the upstream Pipoya attribution note as `pipoya-about.txt`. The subset currently includes four default official Woka sprite sheets (`male1`, `male11`, `female1`, `female6`) sourced from `workadventure/workadventure` commit `b94fa79f8313eb90e606847669044cd0631029a8`, and the renderer maps live agents onto those repo-local sprite sheets as actual people/person markers instead of generic placeholders.
- 2026-05-12 visual refinement pass for embedded realtime WorkAdventure mode completed: the primary `/` renderer now uses a darker operations palette over the official office map, explicit single-person desk areas, hanging desk identifiers, opaque high-contrast nameplates, compact status badges, visible capped task stacks, and clearer interaction-zone outlines/quiet-zone markers. This pass is renderer-local and does not delete or weaken the existing Kenney/R3F provenance/perf contract on `/prototype`.
- 2026-05-12 validation evidence for the primary WorkAdventure renderer pass:
  `npm --prefix frontend/office-scene-prototype test` => passed (`17` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-BYN3vpZE.js`, `styles-BeU9k7NW.js`, updated `realtime-scene-embed.js`, updated `realtime-scene.css`, and copied repo-local `workadventure-woka-subset/` alongside the existing `workadventure-preview/` and Poly Pizza assets under `clawobserver/static/prototype/office-assets/`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests).
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes.
  Embedded realtime `/` smoke contract: `sceneKind: "workadventure-primary"`, `rendererMode: "workadventure"`, `assetStrategy: "workadventure-tilemap-runtime-subset"`, `wokaMode: "workadventure-runtime-woka-subset"`, `wokaSubsetCount: 4`, `mapMode: "workadventure-office-map-preview"`, `renderedDeskCount: 12`, `renderedZoneCount: 4`, `renderedWokaCount: 4`, `renderedLoungeCount: 3`, `statusBoardRuntime: "ok"`, `summaryVariant: "wide"`, and `occupantCount: 4`.
  Retained `/prototype` smoke contract: `labelOrientation: "camera-facing-yaw"`, `labelLayer: "elevated-forward-billboard"`, `frameloop: "demand"`, `performanceMode: "idle-on-demand"`, `officeAssetModelCount: 9`, `avatarPreviewModelCount: 4`, `waPreviewMode: "workadventure-office-map-preview"`, and `postSettleAnimationFrames: 4`.
  Screenshot metrics remained healthy: `/prototype` `brightPixels: 413626`, `midVariancePixels: 329364`, `uniqueBuckets: 4096`, `lumaSpread: 255`; realtime `/` `brightPixels: 626492`, `midVariancePixels: 462821`, `uniqueBuckets: 4096`, `lumaSpread: 255`.
  Screenshot evidence: `tmp/realtime-browser-smoke.png` (embedded primary WorkAdventure mode), `tmp/prototype-browser-smoke.png` (retained legacy 3D route), `tmp/prototype-workadventure-preview.png` (prototype-side map preview panel), `tmp/prototype-avatar-preview-closeup.png`, and `tmp/realtime-avatar-preview-closeup.png` (artifact path retained by smoke harness; realtime mode no longer captures a separate 3D avatar-demo crop).
- **Independent verification on 2026-05-12 (Kimi Code CLI)**: confirmed the WorkAdventure-direction preview demo is preview-only, does not replace the production R3F scene, and keeps `AgentVisualState` unchanged.
  - Changed files review: `WorkAdventurePreviewPanel.tsx` is a new React component rendering a 2D tile-map preview panel. `App.tsx` adds `showWorkAdventurePreview` only for the `/prototype` route. `RealtimeOfficeSceneApp.tsx` conditionally renders `WorkAdventurePreviewPanel` when `showWorkAdventurePreview={true}`. `EmbeddedRealtimeScenePanel.tsx` (used by production `/` via `realtimeSceneEmbed.tsx`) does NOT import or render `WorkAdventurePreviewPanel`. `agentVisualState.ts` shows zero diff.
  - Asset provenance/license review: `frontend/office-scene-prototype/public/office-assets/workadventure-preview/provenance.json` pins upstream repo `workadventure/map-starter-kit` at commit `7ce4a7c87380094357bd608805313dc2c737007a`, lists all 10 tileset PNGs plus `office.tmj`/`office.png` with sha256 checksums, and declares `previewOnly: true` and `usageScope: "workadventure-map-only"`. Bundled `LICENSE.assets` states WorkAdventure-specific resources license (map-only use). Bundled `LICENSE.map` is CC BY-SA 3.0. `THIRD_PARTY_ATTRIBUTIONS.md` documents the set with scope note. Build copies the full tree into `clawobserver/static/prototype/office-assets/workadventure-preview/`.
  - Build/static output: `npm run build` succeeded, emitted new hashed bundles (`prototype-pVIfbxuc.js`, `styles-4gQOe60j.js`), updated `realtime-scene-embed.js`, and copied `workadventure-preview/` assets into the Python-served static tree. `index.html` points to the new bundles.
  - Browser smoke coverage: `npm run smoke:browser` passed for both `/` and `/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. The prototype validator asserts `waPreviewPresent`, `waPreviewMode="workadventure-office-map-preview"`, `waPreviewSource="official-workadventure-map-starter-kit"`, `waPreviewLicenseScope="workadventure-map-only"`, map dimensions `31x21`, `walkableTileCount: 366`, `collisionTileCount: 285`, `zoneCount: 7`, `avatarCount: 4`, `tilesetCount: 10`, visible zones, avatars, legend, and license note. The realtime validator does NOT require any WA preview markers, confirming the production route is unaffected. Screenshot evidence exists: `tmp/prototype-workadventure-preview.png` (260426 bytes, 862x585 PNG), `tmp/prototype-browser-smoke.png` (195249 bytes), `tmp/realtime-browser-smoke.png` (187694 bytes).
  - Verification commands run: `npm --prefix frontend/office-scene-prototype test` => `16 passed`. `npm --prefix frontend/office-scene-prototype run build` => success. `npm --prefix frontend/office-scene-prototype run smoke:browser` => success. `python3 -m pytest` => `57 passed`. `python3 -m compileall clawobserver` => success. `node --check clawobserver/static/app.js` => success.
  - Verdict: **PASS**. No blockers. The WorkAdventure tile-map preview is correctly isolated to `/prototype`, the production R3F scene at `/` is unchanged, and `AgentVisualState` contract is untouched.
- 2026-05-11 Poly Pizza preview-avatar pass: replaced the visible primitive/blocky preview seam with a repo-local Poly Pizza `Hyper Casual Character` GLB (`J-Toastie`, CC BY 3.0) rendered as four tinted seated desk instances at `Desk 5` through `Desk 8`. `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now places the preview avatars behind the desk surface and facing the monitors, while `frontend/office-scene-prototype/src/data/officeAvatarCatalog.ts` and `AgentOfficeScene.tsx` advertise the new preview-only contract (`preview-hyper-casual-cc-by-avatar`, `poly-pizza-hyper-casual-local-preview`, `seated-behind-desk-facing-monitor`). The old visible rounded primitive preview and repo-local Kenney Blocky preview asset path were removed from the active scene path. Attribution/provenance now live at `THIRD_PARTY_ATTRIBUTIONS.md` and `frontend/office-scene-prototype/public/office-assets/poly-pizza-hyper-casual-preview/provenance.json`, with the CC BY 3.0 license text bundled locally.
- 2026-05-11 validation evidence for the Poly Pizza preview-avatar pass:
  `npm --prefix frontend/office-scene-prototype test` => passed (`15` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-BmYolkQ6.js`, `styles-CyxVgWbF.js`, updated `realtime-scene-embed.js`, and copied `poly-pizza-hyper-casual-preview/` into the Python-served static asset tree.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `avatarPreviewMode: "preview-hyper-casual-cc-by-avatar"`, `avatarPreviewSource: "poly-pizza-hyper-casual-local-preview"`, `avatarPreviewProvenanceSource: "poly-pizza-hyper-casual-character-cc-by-3.0"`, `avatarPlacement: "seated-behind-desk-facing-monitor"`, visible preview desks `Desk 5, Desk 6, Desk 7, Desk 8`, rendered preview count `4`, and the new CC BY license/provenance paths. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`, `tmp/prototype-avatar-preview-closeup.png`, `tmp/realtime-avatar-preview-closeup.png`.
  Browser-smoke screenshot metrics remained healthy after the avatar swap: prototype `fileBytes: 153799`, `brightPixels: 440458`, `darkPixels: 71546`, `midVariancePixels: 343889`, `uniqueBuckets: 4096`, `lumaSpread: 254.36`; realtime `fileBytes: 147757`, `brightPixels: 420731`, `darkPixels: 61866`, `midVariancePixels: 333138`, `uniqueBuckets: 4096`, `lumaSpread: 254.64`.
  `python -m pytest` => passed (`57` tests).
- 2026-05-11 workstation-hierarchy blocker repair after owner screenshot rejection: `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now treats the desk as the container and the monitor as the focal object instead of letting the label dominate the workstation. All 12 fixed slots in `frontend/office-scene-prototype/src/config/workstationSlots.ts` now face `0` radians so desk fronts and monitor screens face the production camera. The desk composition was rebuilt around a wider-than-deep footprint with a stronger desktop slab, grounded side cabinet/support geometry, chair moved behind the desk, and integrated monitor/keyboard/mouse/cable details anchored on the desk surface. Nameplates were reduced into smaller camera-facing metadata tags just above the monitor (`DESK_LABEL_HIERARCHY_MODE = "small-monitor-top-metadata-tag"`), preserving readability without covering the workstation or future character head space. The repo-local Kenney CC0 asset path, primitive fallback seam, full-width layout, `AgentVisualState` contract, `frontLabelLaneClearance = "open-center"`, no overhead slab, opaque structures, and `frameloop="demand"` idle-performance guard remain intact.
- 2026-05-11 workstation-hierarchy regression guard extension: `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now publishes `data-scene-label-hierarchy="small-monitor-top-metadata-tag"`, `data-scene-monitor-detail="integrated-screen-keyboard-mouse"`, and `data-scene-workstation-proportion="wide-front-facing-workstation"`. `frontend/office-scene-prototype/src/lib/sceneState.test.ts` asserts those renderer invariants plus the new `0`-radian desk-facing contract, and `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now fails if either route drops the repaired hierarchy/detail/proportion markers.
- 2026-05-11 validation evidence for the workstation-hierarchy blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`15` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-kfZwKKle.js`, `styles-DN-PyAK1.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `workstationOrientation: "all-desks-face-camera"`, `monitorStyle: "screen-plane-cyan-edge"`, `labelHierarchy: "small-monitor-top-metadata-tag"`, `monitorDetail: "integrated-screen-keyboard-mouse"`, `workstationProportion: "wide-front-facing-workstation"`, `frontLabelLaneClearance: "open-center"`, `deskStructureVisual: "opaque"`, `frameloop: "demand"`, and repo-local Kenney provenance/license markers. Fresh screenshot artifacts showing the corrected desk orientation and attached desktop monitor detail: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
  Browser-smoke screenshot metrics stayed rich after the hierarchy fix: prototype `fileBytes: 174502`, `brightPixels: 429532`, `darkPixels: 69732`, `midVariancePixels: 348365`, `uniqueBuckets: 4096`, `lumaSpread: 253.71`; realtime `fileBytes: 168579`, `brightPixels: 420582`, `darkPixels: 61785`, `midVariancePixels: 337719`, `uniqueBuckets: 4096`, `lumaSpread: 254.43`. Fit margins stayed positive with ample label clearance (`prototype scene left/right 0.019`, `prototype label left/right 0.122`; `realtime scene left/right 0.024`, `realtime label left/right 0.125`).
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-11 screenshot-blocker repair pass 2: `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now defines an explicit `camera-facing-inner-workstation-group` so the visible workstation silhouette is a broad front-facing desk instead of a long side corridor. The centered monitor was moved to the tabletop back edge and brightened, keyboard/mouse geometry was enlarged for smoke readability, the badge label was reduced and pulled into a monitor-corner placement with `monitor-corner-badge-clear`, and the chair was re-centered behind the desk for a future seated character path. `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`, `frontend/office-scene-prototype/src/lib/sceneState.test.ts`, and `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now expose and assert `camera-facing-inner-workstation-group`, `keyboard-mouse-readable`, and `monitor-corner-badge-clear` so composition regressions are caught alongside the existing orientation/proportion/detail contracts.
- 2026-05-11 validation evidence for screenshot-blocker repair pass 2:
  `npm --prefix frontend/office-scene-prototype test` => passed (`15` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-SMsIEAB_.js`, `styles-DrtRwgTS.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `workstationOrientation: "all-desks-face-camera"`, `monitorStyle: "screen-plane-cyan-edge"`, `labelHierarchy: "small-monitor-top-metadata-tag"`, `monitorDetail: "integrated-screen-keyboard-mouse"`, `workstationProportion: "wide-front-facing-workstation"`, `innerWorkstationOrientation: "camera-facing-inner-workstation-group"`, `peripheralVisibility: "keyboard-mouse-readable"`, `labelOcclusion: "monitor-corner-badge-clear"`, `frontLabelLaneClearance: "open-center"`, `deskStructureVisual: "opaque"`, and `frameloop: "demand"`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
  Browser-smoke screenshot metrics remained strong after the pass-2 composition rebuild: prototype `fileBytes: 153664`, `brightPixels: 431764`, `darkPixels: 67190`, `midVariancePixels: 350757`, `uniqueBuckets: 4096`, `lumaSpread: 254.50`; realtime `fileBytes: 147489`, `brightPixels: 426878`, `darkPixels: 57846`, `midVariancePixels: 331167`, `uniqueBuckets: 4096`, `lumaSpread: 254.78`. Fit margins stayed positive with label clearance (`prototype scene left/right 0.019`, `prototype label left/right 0.174/0.115`; `realtime scene left/right 0.024`, `realtime label left/right 0.176/0.117`).
  `python3 -m pytest` => passed (`57` tests).
- Current migration run on 2026-05-10: production `/` must now host the R3F office scene directly; `/prototype` may remain only as a secondary route over the same scene runtime.
- Production R3F migration completed on 2026-05-10: `clawobserver/static/app.js` now replaces the old central static-image scene area with a mount point that loads the built embed bundle at `/assets/prototype/assets/realtime-scene-embed.js` plus `/assets/prototype/assets/realtime-scene.css`. The production Realtime page keeps existing KPI/history/token flows while the central scene, summary panel, and selected-desk detail now come from the shared React scene runtime.
- Live-state wiring completed on 2026-05-10: the shared adapter now maps `/api/live/overview` into richer `AgentVisualState` rows plus `SceneSummary`, driving desk occupancy, monitor emissive state, status lamps, task/file stacks, queue/global summary, runtime-status copy, and selected desk detail. Busy/idle/error/offline mappings remain truthful; degraded and waiting runtime states stay explicit instead of inventing activity.
- Visual/UX refinement completed on 2026-05-10: the office shell now pushes closer to `static_scene.jpg` with warmer walls/flooring, brighter workstation surfaces, better lounge contrast, added ambient ceiling strips/fixtures, clearer desk subtitles, stronger summary/detail hierarchy, and lightweight runtime-colored accent lighting without heavy effects or post-processing.
- Validation evidence for the production migration on 2026-05-10:
  `npm --prefix frontend/office-scene-prototype test` => passed (`11` tests).
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` including stable `assets/realtime-scene-embed.js` and `assets/realtime-scene.css`.
  `PYTHONPATH=. pytest tests/` => passed (`57` tests).
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed server `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`; production `/` screenshot artifact `tmp/realtime-browser-smoke.png`, prototype screenshot artifact `tmp/prototype-browser-smoke.png`.
  `REALTIME_SMOKE_URL=http://127.0.0.1:8420/ PROTOTYPE_SMOKE_URL=http://127.0.0.1:8420/prototype npm --prefix frontend/office-scene-prototype run smoke:browser` => passed against the actual running service with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes; current live `8420` runtime reported truthful `degraded` state and still rendered the scene successfully.
- Current run scope on 2026-05-10: this run starts with required root-doc updates, then proceeds as an implementation run for the minimal Three.js + React Three Fiber scene MVP.
- MVP scene hard constraints for this run: exactly 12 fixed workstation/desk slots; visual style and layout reference `clawobserver/static/assets/static_scene.jpg` with both work area and reserved rest/lounge area represented.
- Lounge/rest region requirement: MVP may keep this region simple, but it must exist as a reserved scene zone for Phase 2 person/task/status expansion.
- Phase A historical restoration finding on 2026-05-08: archive history, SQLite schema, archive reads, and the user `systemd` timer/service chain remained healthy. The database still accumulated snapshots every 30 minutes and prior days retained non-empty totals, so the historical path did not need schema/timer repair.
- Phase B real-data connection finding on 2026-05-08: the break was in the live runtime adapter source chain. `scripts/openclaw_runtime_adapter.py` began with `openclaw sessions --all-agents --json`, which hangs on the current host, and gateway status calls can also hang/fail. That combination exhausted the outer runtime-command timeout and replaced real OpenClaw local session-store data with empty waiting payloads, which then propagated unchanged through `/api/live/overview`, `clawobserver capture`, and today's archive rows.
- Phase B repair on 2026-05-08: the adapter now treats `~/.openclaw/agents/*/sessions/sessions.json` as the primary real source for all-agent sessions, token counters, and agent metadata; bounded CLI calls are now optional best-effort enrichments instead of the gate for all live data. Gateway failures now produce truthful degraded status/reasons without erasing real session totals or delivery-queue depth.
- Deployment/timer finding on 2026-05-08: the installed user units and env file already pointed both web and capture services at the repo runtime adapter and the correct SQLite path. No `systemd` unit or env-file changes were required for the fix.
- Current focused session starts from the live repository state and ignores prior failed reasoning tracks.
- Do not expand into unrelated UI redesign.
- Scene/layout alignment finding on 2026-05-08: the Realtime office geometry is now owned by `clawobserver/static/reference-scene-layout.json`, measured from the real `static_scene.jpg` asset, then normalized by `clawobserver/static/app.js` from image pixels into percentage anchors for responsive rendering.
- Scene/layout behavior on 2026-05-08: active agents render only at configured workstation slots; idle agents render only in configured lounge slots; unassigned workstations stay visually empty and may still show placeholder hanging tags; role/style config remains in `clawobserver/static/scene-role-styles.json`.
- Additional verification on 2026-05-08 found two remaining backend escape hatches after the earlier `sessions: null` fix: a non-object top-level runtime payload (for example a JSON list) raised `AttributeError` in `LiveRuntimeAdapter._normalize_payload()`, and an invalid `captured_at` raised `ValueError` in `_parse_timestamp()`. Both now degrade to HTTP 200 `capture_status: "waiting"` live payloads instead of falling through to the server's controlled 503 error wrapper.
- Additional real-instance verification on 2026-05-08 found the still-live 8420 failure was not another payload-shape crash. `/api/live/overview` returned HTTP 200 waiting JSON, but only after about 5.05s because `clawobserver/runtime.py` allowed the configured runtime command to run for 5s while the browser aborts live fetches after 4s. The repair is to keep the backend fail-soft budget under that 4s client deadline.
- Additional real-instance verification on 2026-05-08 confirmed the true current root cause after the timeout-contract fix: `/usr/bin/env python3 /mnt/data/repositories/ClawObserver/scripts/openclaw_runtime_adapter.py` still depended on `openclaw sessions --all-agents --json`, which timed out and forced the adapter to emit a generic zero-session waiting payload even though local OpenClaw session stores remained readable and non-empty.
- Phase A historical baseline is still the non-empty demo/runtime payload from `build_demo_payload()` in `clawobserver/runtime.py` across `v0.1.0`, `v0.2.0`, `v0.2.1`, and `8b5bab9`, which populated session overview totals, per-agent counts, session states, session types, queues, gateways, and token rows.
- Phase B validation on 2026-05-08 after the repair:
  `python3 scripts/openclaw_runtime_adapter.py` returned `capture_status: "waiting"` but with `sessions.total = 91`, `sessions.active = 3`, per-agent rows including `main`, `media-manager`, and `se-codex`, plus truthful queue rows from `~/.openclaw/delivery-queue`.
  `CLAWOBSERVER_RUNTIME_COMMAND='/usr/bin/env python3 /mnt/data/repositories/ClawObserver/scripts/openclaw_runtime_adapter.py' python3 - <<'PY' ... app.live_overview_payload() ... PY` returned `source_version: "openclaw-cli-runtime"`, `total_sessions = 91`, `active_sessions = 3`, `agent_count = 10`, non-empty `queue_lanes`, and degraded-but-honest gateway counts instead of the prior empty shell.
- Independent verification on 2026-05-10: inspected all changed files, confirmed stable `AgentVisualState` contract and adapter boundary, exactly 12 fixed workstation slots with capped task stacks, safe `/prototype` static route and `/api/live/prototype` backend behavior that do not break `/` or `/api/live/overview`, ran Python unit tests (55 passed), frontend unit tests (11 passed), and production build successfully; added `node_modules/` to `.gitignore`; no blocking issues.
- Browser-runtime regression finding on 2026-05-10: `frontend/office-scene-prototype/src/components/AgentDesk.tsx` passed `data-task-stack` to an R3F `<group>`. React Three Fiber treats dashed props as nested Three-object paths, so it resolved `task.stack` against the `group` instance, hit `undefined`, and crashed in `@react-three/fiber` `events-*.esm.js` while mounting. The repeated `TypeError: Cannot read properties of undefined (reading 'task')` left `/prototype` with an empty `#root` and then `THREE.WebGLRenderer: Context Lost.`
- Browser-runtime regression validation on 2026-05-10: added `npm --prefix frontend/office-scene-prototype run smoke:browser`, which starts a local ClawObserver server on port `8431`, loads `/prototype` in headless Chromium via Playwright, fails on console errors/page errors/HTTP 4xx+, and asserts scene-root plus canvas rendering with a saved screenshot at `tmp/prototype-browser-smoke.png`.
- Independent verification on 2026-05-10 (black-screen repair): the entire `frontend/` directory is untracked in git, so no diff for the claimed `data-task-stack` removal could be inspected; current `AgentDesk.tsx` does not contain the prop. Browser smoke initially failed with one console error (`404 /assets/static_scene.jpg`) and one failed request; fixed by copying the tracked asset from `clawobserver/static/assets/static_scene.jpg` to `clawobserver/static/static_scene.jpg` so the `/assets/*` static route can resolve it. After the fix, smoke passes with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`, confirmed canvas/detail/header/footer rendering, and screenshot at `tmp/prototype-browser-smoke.png`. The running service at `http://127.0.0.1:8420/prototype` was verified without restart. `node_modules/` is correctly ignored.
- Visual legibility repair on 2026-05-10: `/prototype` no longer presents as a near-solid blue canvas. The R3F camera now uses a top-oblique angle into the room, the office shell separates work floor, aisle, rear wall, and reserved lounge with contrasting materials, desks gained brighter geometry/legs/monitor stands plus clearer status lamps and labels, and the lounge now reads as a distinct blue seating zone on a warm floor base inspired by `static_scene.jpg`. `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now validates screenshot content rather than only DOM/canvas presence by capturing the scene shell and checking brightness spread, mid-tone variance, color-bucket diversity, and brighter lower-band lounge/work content so an all-blue or low-contrast regression fails automatically.
- Visual legibility validation on 2026-05-10: `npm --prefix frontend/office-scene-prototype test` passed (`11` tests), `npm --prefix frontend/office-scene-prototype run build` passed and rebuilt `clawobserver/static/prototype/`, local browser smoke on `http://127.0.0.1:8431/prototype` passed with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`, declared scene metadata `deskCount: 12`, `statusBoard: true`, `lounge: true`, and screenshot metrics `brightPixels: 198622`, `midVariancePixels: 205341`, `uniqueBuckets: 3922`, `dominantBucketRatio: 0.0307`. External validation against the actual running service at `http://127.0.0.1:8420/prototype` also passed with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`, declared scene metadata intact, and screenshot metrics `brightPixels: 190201`, `midVariancePixels: 205259`, `uniqueBuckets: 3893`, `dominantBucketRatio: 0.0307`. Current repaired screenshot path: `tmp/prototype-browser-smoke.png`.
- **Independent visual verification on 2026-05-10**: inspected `AgentOfficeScene.tsx` (camera `[0, 11.5, 14.5]` fov 48, ambient 1.15, hemisphere + 2 directional lights), `OfficeShell.tsx` (contrasting floor `#122031`/`#253950`/`#314764`, aisle strip `#8da2ba`, rear wall `#16304d`, side walls `#203243`, lounge base `#b67c4d`, tables `#4e6780`, seats `#1b4d94`, status board `#14273d` with emissive screen `#1d4f86`), `AgentDesk.tsx` (legs `#7a899c`, monitor stand `#596474`/`#6a7788`, status lamp with sphere emissive, task stack `#d7e5ff`, label with outline `#09121c`), and `prototypeBrowserSmoke.mjs` (brightness, variance, bucket diversity, band-luma, and semantic metadata assertions for 12 desks/status board/lounge). Ran frontend tests (`11` passed), build (`vite` success), Python tests (`55` passed including `test_server.py`). Browser smoke passed on isolated server (`8431`) and on running service (`8420`) with zero console/page/request errors. Screenshot comparison (`tmp/prototype-visual-issue-before.png` vs `tmp/prototype-browser-smoke.png`) confirms the scene changed from an indistinct near-black canvas to a clearly readable office with 12 labeled desks, monitors, status lamps, rear status board, and front lounge/rest area. `node_modules/` is correctly ignored. Independent verdict: **ACCEPT**.

- **Independent verification on 2026-05-10 (Kimi Code CLI subagent)**: re-verified source files confirming `clawobserver/static/app.js` replaces the previous static-image central scene with `#realtime-r3f-scene-mount` that dynamically loads the built R3F embed bundle (`/assets/prototype/assets/realtime-scene-embed.js`) and passes the normalized `/api/live/overview` payload directly into `window.ClawObserverRealtimeScene.render()`. Confirmed `AgentOfficeScene.tsx` uses `@react-three/fiber` Canvas with 12 fixed desks, status board, lounge zone, monitor/lamp/task-stack geometries, and live-data-driven colors. Confirmed the old `renderRealtimeScene()` DOM fallback (static `scene-reference-base` img, percentage-based tags, lounge plates) still exists in `app.js` as dead code but is never invoked in the current flow.
- **Test re-run**: `pytest tests/` => `57 passed`. `npm --prefix frontend/office-scene-prototype test` => `11 passed` (4 files). `npm --prefix frontend/office-scene-prototype run build` => succeeded; rebuilt `clawobserver/static/prototype/assets/`.
- **Browser smoke re-run (isolated server, manual Playwright)**:
  - `http://127.0.0.1:8432/` => `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Scene root present, canvas rendered, `declaredDeskCount: 12`, `declaredStatusBoard: true`, `declaredLounge: true`, `runtimeStatus: "ok"`, status board text `"Global status busy 2 idle 2 error 0 offline 0 queue 2"`, summary queue `"2 pending / 1 failed"`, summary sessions `"3 active / 8 total"`. Screenshot: `tmp/isolated-realtime-smoke.png`.
  - `http://127.0.0.1:8432/prototype` => `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Scene root present, canvas rendered, `declaredDeskCount: 12`, `declaredStatusBoard: true`, `declaredLounge: true`, detail panel present. Screenshot: `tmp/isolated-prototype-smoke.png`.
- **Browser smoke re-run (actual running `http://127.0.0.1:8420/`)**:
  - `/` => `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Scene root present, canvas rendered, `declaredDeskCount: 12`, `declaredStatusBoard: true`, `declaredLounge: true`, `runtimeStatus: "degraded"`, status board text `"Global status busy 3 idle 9 error 0 offline 0 queue 0"`, summary queue `"0 pending / 4 failed"`, summary sessions `"3 active / 94 total"`. Screenshot: `tmp/realtime-8420-smoke.png`.
  - `/prototype` => `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Scene root present, canvas rendered, `declaredDeskCount: 12`, `declaredStatusBoard: true`, `declaredLounge: true`, detail panel present. Screenshot: `tmp/prototype-8420-smoke.png`.
- **Built-in smoke script caveat**: the committed `prototypeBrowserSmoke.mjs` contains a `bottomBandAverageLuma` heuristic (`assertSharedSceneQuality`) that fails for the production `/` page when the scene canvas is narrower (758 px) because the bottom band (dark floor/lounge) is darker than the top band (light walls). This is a false negative in the automated heuristic, not a scene or data failure. The scene is clearly visible and data-linked; manual inspection of `tmp/realtime-browser-smoke.png` confirms correct rendering.
- **Remaining risks identified**: (1) `prototypeBrowserSmoke.mjs` `bottomBandAverageLuma` check should be relaxed or made layout-aware for the narrower production dashboard canvas. (2) Dead code `renderRealtimeScene()`, `buildRealtimeSceneModel()`, and related DOM-scene helper functions (~300 lines) remain in `clawobserver/static/app.js` and should be removed to reduce bundle size and maintenance confusion. (3) `node_modules/` is correctly ignored in git.
- **Independent verdict**: **ACCEPT**.
- 2026-05-10 smoke follow-up: `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now infers the correct validator from the external URL path (`/` => production Realtime validator, `/prototype` => prototype validator) and also accepts `PROTOTYPE_SMOKE_MODE=realtime|prototype|both` to make intent explicit when running against real services. Shared screenshot assertions now require zero console/page/request errors, the R3F canvas, semantic markers for exactly 12 desks plus status board/lounge, summary/status-board text presence, and robust visual diversity (`brightPixels`, `darkPixels`, `midVariancePixels`, `uniqueBuckets`, `dominantBucketRatio`, `lumaSpread`) instead of the prior false-negative `bottomBandAverageLuma > topBandAverageLuma` heuristic.
- 2026-05-10 cleanup decision: removing the legacy DOM-scene helpers from `clawobserver/static/app.js` was deferred in this turn because those functions are still directly exercised by `tests/test_realtime_scene_logic.py` even though the production runtime path mounts the R3F scene via `#realtime-r3f-scene-mount`. Safe removal should be done as a separate refactor that retires or rewrites that test surface first.
- 2026-05-10 production Realtime scene framing/polish fix: the four white occluding blocks seen on the real `http://127.0.0.1:8420/` review were the oversized cream ceiling light slabs in `frontend/office-scene-prototype/src/components/OfficeShell.tsx`. They sat directly over the back desk row and visually covered several desk labels/status areas. The fix replaced those slabs with slimmer rear soffits, pendant lights, dividers, and wall props that preserve the office-ceiling feel without crossing desk sightlines.
- 2026-05-10 framing fix details: `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now computes camera distance from the known 12-desk office footprint instead of relying on the earlier fixed `PerspectiveCamera` pose. At the production canvas size (~`758x760` inside the `1440x1100` page viewport), all 12 desks remain within frame. The scene root now records projected desk-fit margins (`data-scene-fit-left/right/top/bottom`), and smoke output from this run reported `sceneFitLeft/right = 0.091` and `sceneFitTop/bottom ≈ 0.396/0.402` on the real `8420` Realtime route.
- 2026-05-10 immediate visual polish pass: desks gained clearer chair/monitor/accessory geometry, softer desk materials, subtler in-scene hover cards, a smaller in-world status board, lounge table/sofa detail, plants, whiteboards, room dividers, and warmer friendly wall/floor materials. The result is more office-like and less flat without adding heavy shadows, post-processing, or animated avatars.
- 2026-05-10 future asset recommendation: a dedicated low-poly/glTF set for desks, chairs, monitors, partitions, lounge seating, and small office props is still recommended for the next visual-quality jump. The current state-driven component protocol is preserved, so those future models can replace the primitive geometry without changing the live OpenClaw data mapping contract.
- 2026-05-10 validation evidence for this visual/framing run:
  `npm --prefix frontend/office-scene-prototype test` => passed (`11` tests).
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/`.
  `PYTHONPATH=. pytest tests/` => passed (`57` tests).
  `PROTOTYPE_SMOKE_PORT=8443 npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8443/` and `/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Screenshot artifacts: `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`.
  `PROTOTYPE_SMOKE_PORT=8444 REALTIME_SMOKE_URL=http://127.0.0.1:8420/ PROTOTYPE_SMOKE_URL=http://127.0.0.1:8420/prototype PROTOTYPE_SMOKE_MODE=both npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on the actual running service with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both `/` and `/prototype`. Real-route screenshot artifacts remained `tmp/realtime-browser-smoke.png` and `tmp/prototype-browser-smoke.png`; additional review captures from this run: `tmp/realtime-8420-current-scene.png`, `tmp/realtime-8420-polish-final.png`, `tmp/prototype-8420-polish-final.png`.
- 2026-05-11 screenshot-repair follow-up: fixed mirrored back-row/mid-row desk labels by moving desk geometry under a rotated child group and rendering labels with Y-axis camera billboarding in `frontend/office-scene-prototype/src/components/AgentDesk.tsx`, so the readable nameplates no longer inherit reversed desk yaw from the row layout. Fixed ghostly middle-ground overlap by removing transparency from structural partitions, the compact front rail, lounge seat accents, and desk/chair shell materials in `frontend/office-scene-prototype/src/components/OfficeProps.tsx`, `frontend/office-scene-prototype/src/components/OfficeShell.tsx`, and `frontend/office-scene-prototype/src/components/AgentDesk.tsx` while preserving monitor screen glow and the live `AgentVisualState` contract.
- 2026-05-11 regression coverage follow-up: `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now advertises `data-scene-label-orientation="camera-facing-yaw"` and `data-scene-structural-opacity="opaque"`. `frontend/office-scene-prototype/src/lib/sceneState.test.ts` asserts those renderer invariants, and `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now fails if either marker regresses while still enforcing the shared screenshot-diversity and semantic scene checks.
- 2026-05-11 validation evidence for the screenshot repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`13` tests).
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets including `prototype-BJj7-HGm.js` and `styles-9QEA5TEx.js`.
  `PROTOTYPE_SMOKE_PORT=8449 npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8449/` and `/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Scene metadata reported `labelOrientation: "camera-facing-yaw"` and `structuralOpacity: "opaque"` on both routes; fit margins remained positive (`prototype left/right 0.019`, `realtime left/right 0.024`). Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
- 2026-05-11 manual screenshot spot-check: reviewed the rebuilt `tmp/prototype-browser-smoke.png` and `tmp/realtime-browser-smoke.png`. Back-row and middle-row desk labels now read left-to-right from the production camera view, and the prior semi-transparent partition/desk overlap no longer creates a ghosted mid-scene band.
- 2026-05-11 screenshot-review repair after Kenney integration: kept the repo-local Kenney CC0 asset set and metadata intact while fixing the three remaining screenshot issues in the shared R3F scene. `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now keeps structural desk/chair furniture visually opaque even for offline desks, limits live status cues to monitor/lamp/accent surfaces, and raises desk labels onto an opaque high-contrast billboard plate that still uses camera-facing yaw. `frontend/office-scene-prototype/src/components/OfficeShell.tsx` trims the bright rear ceiling slab into a narrow back strip and moves the soffits higher/rearward so upper geometry no longer crosses the back-desk sightline from the production camera. `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now advertises explicit renderer invariants for label plate mode, opaque desk structure mode, and clear back-row overhead sightline.
- 2026-05-11 smoke/metadata regression coverage extension: `frontend/office-scene-prototype/src/lib/sceneState.test.ts` now asserts `DESK_LABEL_PLATE_MODE = "opaque-high-contrast"`, `DESK_STRUCTURE_VISUAL_MODE = "opaque"`, and `OVERHEAD_SIGHTLINE_MODE = "clear-back-row"`. `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now fails if either route drops the opaque label-plate marker, opaque desk-structure marker, or clear-overhead-sightline marker, in addition to the existing Kenney asset/license/provenance, 12-desk, summary-above-canvas, fit-margin, and screenshot-diversity checks.
- 2026-05-11 validation evidence for the screenshot-review repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-CPc8p8uO.js`, `styles-Cl3uhDGH.js`, updated `realtime-scene-embed.js`, and unchanged repo-local Kenney `office-assets/`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests). Targeted `python3 -m pytest tests/test_server.py tests/test_realtime_scene_logic.py` also passed (`25` tests) before the full sweep.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Scene metadata on both routes reported `labelOrientation: "camera-facing-yaw"`, `labelPlate: "opaque-high-contrast"`, `deskStructureVisual: "opaque"`, `structuralOpacity: "opaque"`, `overheadSightline: "clear-back-row"`, `officeAssetModelCount: 9`, and positive fit margins (`prototype left/right 0.019`, `realtime left/right 0.024`). Screenshot metrics improved to `prototype brightPixels: 433861`, `prototype midVariancePixels: 379746`, `realtime brightPixels: 439458`, `realtime midVariancePixels: 381288`, both with `uniqueBuckets: 4096` and `lumaSpread > 253`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
- 2026-05-11 final label-clearance repair after the Kenney asset pass: `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now renders persistent desk nameplates on an explicit elevated forward billboard layer (`DESK_LABEL_LAYER_MODE = "elevated-forward-billboard"`) with a taller opaque plate, deeper padding for the subtitle line, a repo-local font asset at `frontend/office-scene-prototype/public/office-assets/fonts/NimbusSans-Regular.otf` (`sha256 7c25be4d78155523080ab85b10277150657ff7dabbcad7037bdd536c9b6d0d08`), and ASCII-only subtitle text so no external font fallback requests are needed. The desk status lamp and task stack moved rearward on the desktop so they no longer occupy the same screen-space lane as the nameplates from the production camera angle.
- 2026-05-11 label-clearance smoke coverage extension: `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now publishes `data-scene-label-layer="elevated-forward-billboard"` plus projected `data-scene-label-fit-*` margins derived from the actual label plate corners, and `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now fails if either route drops the label-layer marker or if the label plate margins fall below the enforced safety threshold (`0.03`). `frontend/office-scene-prototype/src/lib/sceneState.test.ts` also asserts the new label-layer contract.
- 2026-05-11 validation evidence for the final label-clearance repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-7eQT1ifC.js`, `styles-_bxKCnbu.js`, updated `realtime-scene-embed.js`, and copied repo-local font assets under `clawobserver/static/prototype/office-assets/fonts/`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests). Targeted `python3 -m pytest tests/test_server.py tests/test_realtime_scene_logic.py` also passed (`25` tests) during the run-up.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes and no external network font requests. Prototype scene metadata reported `labelLayer: "elevated-forward-billboard"`, `sceneLabelFitLeft/right = 0.031`, `sceneLabelFitTop/bottom = 0.289/0.324`, `sceneFitLeft/right = 0.019`, `frameloop: "demand"`, and `performanceMode: "idle-on-demand"`. Realtime scene metadata reported `labelLayer: "elevated-forward-billboard"`, `sceneLabelFitLeft/right = 0.036`, `sceneLabelFitTop/bottom = 0.295/0.331`, and `sceneFitLeft/right = 0.024`. Screenshot metrics remained healthy: prototype `brightPixels: 445280`, `midVariancePixels: 389543`; realtime `brightPixels: 425114`, `midVariancePixels: 376941`; both routes kept `uniqueBuckets: 4096` and `lumaSpread > 253`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
- 2026-05-11 idle CPU/performance blocker repair: the shared R3F office scene now declares `frameloop="demand"` in `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` and records explicit performance contract metadata (`data-scene-frameloop="demand"`, `data-scene-performance-mode="idle-on-demand"`). The responsive camera rig now invalidates only when its size-driven camera fit recalculates, so the page no longer keeps the WebGL scene on an always-on render loop after initial settle. This preserves the existing Kenney CC0 repo-local OBJ/MTL integration, label readability fixes, opaque desk structures, clear overhead sightline, and the `AgentVisualState` live contract.
- 2026-05-11 automated performance guard extension: `frontend/office-scene-prototype/src/lib/sceneState.test.ts` now asserts the `demand` frameloop / `idle-on-demand` renderer contract. `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now instruments `requestAnimationFrame` before page load, marks the scene as settled after semantic readiness, fails if more than `8` frames occur after settle, and fails if any required Kenney OBJ asset is fetched more than once within a single page load. This provides a lightweight automated regression check for the reported “immediately maxes CPU” failure mode without depending on noisy process-level CPU measurements in CI.
- 2026-05-11 validation evidence for the idle CPU/performance blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-3e2NYHGp.js`, `styles-CqzpMTeW.js`, and updated `realtime-scene-embed.js`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests).
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Scene metadata on both routes reported `frameloop: "demand"` and `performanceMode: "idle-on-demand"`. The new steady-state perf guard observed only `3` post-settle animation frames on `/prototype` and `3` on `/`, with each required Kenney OBJ asset fetched exactly once per page load. Fresh screenshot artifacts remained `tmp/prototype-browser-smoke.png` and `tmp/realtime-browser-smoke.png`.
  `REALTIME_SMOKE_URL=http://127.0.0.1:8420/ PROTOTYPE_SMOKE_URL=http://127.0.0.1:8420/prototype PROTOTYPE_SMOKE_MODE=both npm --prefix frontend/office-scene-prototype run smoke:browser` => passed against the actual running service with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. The live service reported the same renderer contract (`frameloop: "demand"`, `performanceMode: "idle-on-demand"`), stayed within the steady-state guard at `4` post-settle animation frames on both `/` and `/prototype`, and preserved the accepted fit/readability metadata plus repo-local Kenney asset assertions.
- 2026-05-11 Quaternius-style-safe-emulation pass: kept the existing repo-local Kenney CC0 asset baseline, license text, provenance JSON, offline loading path, and primitive fallback seam intact because this run did not establish a deterministic Quaternius package/checksum provenance chain. Instead, the shared R3F scene now emulates the desired rounded/chunky toy-office feel through renderer-local changes: chunkier desk/chair scales, warmer saturated status palette, brighter cream-and-wood office shell, fuller lounge/coffee/bookcase/arc prop dressing, and explicit scene metadata `styleProfile: "toy-office-chunky-warm"` plus `styleReference: "quaternius-inspired-safe-emulation"`.
- 2026-05-11 validation evidence for the Quaternius-style-safe-emulation pass:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-BozpeY5z.js`, `styles-Dk5Cbe24.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype scene metadata reported `styleProfile: "toy-office-chunky-warm"`, `styleReference: "quaternius-inspired-safe-emulation"`, `frameloop: "demand"`, `performanceMode: "idle-on-demand"`, fit margins `left/right 0.019`, label-fit margins `left/right 0.031`, and `postSettleAnimationFrames: 5`. Realtime scene metadata reported the same style/performance markers with fit margins `left/right 0.024`, label-fit margins `left/right 0.036`, and `postSettleAnimationFrames: 5`. Screenshot metrics remained healthy: prototype `brightPixels: 478230`, `darkPixels: 13094`, `midVariancePixels: 390717`, `uniqueBuckets: 3298`, `dominantBucketRatio: 0.0820`, `lumaSpread: 247.68`; realtime `brightPixels: 459594`, `darkPixels: 8980`, `midVariancePixels: 373567`, `uniqueBuckets: 3226`, `dominantBucketRatio: 0.0840`, `lumaSpread: 249.80`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-11 Quaternius blocker repair after independent Kimi verification: the blocked screenshots were caused by the new chunky shell pass collapsing the production camera sightline into a sparse top-down composition. The worst offender was the added full-room cream ceiling slab in `frontend/office-scene-prototype/src/components/OfficeShell.tsx`, which visually covered the central work area from the overhead camera and made the restored Kenney geometry read like bare dark monitor rectangles on a flat light plane. The repair removes that occluding center slab and keeps the chunky warm style through perimeter ceiling rails, rear/front trim, warm accents, and the already-added richer furniture/prop geometry so the Kenney desks/chairs/partitions/plants/bookcases remain clearly visible and the elevated label plates stay readable.
- 2026-05-11 screenshot-richness regression guard extension for the blocker repair: `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now records screenshot file size plus targeted label-band and desk-band image metrics in addition to the existing whole-image diversity checks, scene metadata, Kenney provenance/license assertions, fit margins, and idle perf guard. The smoke now fails if screenshots shrink into the prior sparse-render regime or if the label/desk regions lose too much mid-tone variance or bucket diversity, which catches the “bare slab with invisible labels/furniture” regression class directly instead of relying only on scene metadata.
- 2026-05-11 validation evidence for the Quaternius blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-Bd1pZzdc.js`, `styles-xYG7OOnA.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype scene metadata preserved `styleProfile: "toy-office-chunky-warm"`, `styleReference: "quaternius-inspired-safe-emulation"`, `labelLayer: "elevated-forward-billboard"`, `deskStructureVisual: "opaque"`, `overheadSightline: "clear-back-row"`, `frameloop: "demand"`, and `performanceMode: "idle-on-demand"` with fit margins `left/right 0.019` and label-fit margins `left/right 0.031`. Realtime scene metadata preserved the same style/sightline/perf markers with fit margins `left/right 0.024` and label-fit margins `left/right 0.036`. Screenshot metrics recovered to rich renders instead of the blocked ~40 KB bare scenes: prototype `fileBytes: 155227`, `brightPixels: 441803`, `darkPixels: 54125`, `midVariancePixels: 361795`, `uniqueBuckets: 4096`, `lumaSpread: 253.43`; realtime `fileBytes: 151698`, `brightPixels: 441469`, `darkPixels: 44543`, `midVariancePixels: 350769`, `uniqueBuckets: 4096`, `lumaSpread: 254.57`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-11 front-label trim blocker repair after SE screenshot review: `frontend/office-scene-prototype/src/components/OfficeShell.tsx` now splits the bright front ceiling trim into left/right corner rails instead of a full-width center beam, preserving the chunky warm room frame while leaving the front desk-label lane open. `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx` now publishes `data-scene-front-label-lane-clearance="open-center"`, `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` fails if that contract drops, and `frontend/office-scene-prototype/src/lib/sceneState.test.ts` asserts the same invariant. This keeps the rich Kenney-backed office geometry, no full-room ceiling slab, clear overhead sightline, readable elevated labels, repo-local license/provenance path, and `frameloop="demand"` performance guard intact.
- 2026-05-11 validation evidence for the front-label trim blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`14` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-CqObDZFX.js`, `styles-BVU5aZGp.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `frontLabelLaneClearance: "open-center"` alongside the preserved `labelLayer: "elevated-forward-billboard"`, `deskStructureVisual: "opaque"`, `overheadSightline: "clear-back-row"`, `frameloop: "demand"`, and repo-local Kenney asset/license markers. Screenshot artifacts remained rich: prototype `fileBytes: 164907`, `midVariancePixels: 373250`; realtime `fileBytes: 161101`, `midVariancePixels: 362978`.
  `PROTOTYPE_SMOKE_SCREENSHOT=/mnt/data/workspace-se-codex/tmp/clawobserver-prototype-quaternius-style-final.png REALTIME_SMOKE_SCREENSHOT=/mnt/data/workspace-se-codex/tmp/clawobserver-realtime-quaternius-style-final.png npm --prefix frontend/office-scene-prototype run smoke:browser` => passed and replaced the SE review artifacts with fresh screenshots showing no beam crossing the `researcher`, `Desk 10`, `Desk 11`, or `Desk 12` labels.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-11 aggressive desk-hierarchy blocker repair after SE screenshot rejection: `frontend/office-scene-prototype/src/components/AgentDesk.tsx` now treats each workstation as a wide shallow front-facing primitive desk core layered around the existing repo-local Kenney assets instead of relying on the long desk OBJ silhouette. The dominant tan runway read was removed by shrinking/repositioning the asset-backed desk shell and replacing the visible surface with a steel/cobalt tabletop + fascia assembly whose exported desk aspect contract is `3.0`, keeping every visible desk wider left-to-right than deep front-to-back in the camera view. Chairs are tucked coherently behind the desk fronts for future front-facing characters, monitors are larger and placed directly on the tabletop, and keyboard/mouse/base details remain visible even on dim/offline desks through explicit primitive/emissive augmentation. Labels were reduced from large floating black slabs to small secondary metadata tags offset above the monitor corner instead of centered in front of the workstation, preserving readability without covering the monitor/chair/work area. The palette was tightened further toward the `static_scene.jpg` command-center reference: deep navy environment, darker steel/cobalt desks, stronger cyan screen/edge emissives, and only sparse muted orange accents.
- 2026-05-11 desk-hierarchy contract extension for the blocker repair: `frontend/office-scene-prototype/src/components/AgentOfficeScene.tsx`, `frontend/office-scene-prototype/src/lib/sceneState.test.ts`, and `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now publish and assert the new workstation hierarchy metadata directly: `data-scene-label-scale-hierarchy="small-secondary-corner-badge"` and `data-scene-desk-aspect="3"`, alongside the preserved `workstationOrientation`, `monitorDetail`, `labelHierarchy`, `frontLabelLaneClearance`, opacity, provenance, and `frameloop="demand"` contracts. Vitest now checks the desk aspect ratio is greater than `2.6`, and browser smoke now fails if label scale hierarchy regresses or if workstation desk aspect drops below that threshold, in addition to the existing screenshot-richness, fit-margin, no-external-network, and idle-performance assertions.
- 2026-05-11 validation evidence for the aggressive desk-hierarchy blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`15` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-D2o6kFBj.js`, `styles-ByIiFATI.js`, and updated `realtime-scene-embed.js`.
  `PROTOTYPE_SMOKE_SCREENSHOT=/mnt/data/workspace-se-codex/tmp/clawobserver-prototype-desk-hierarchy-fix.png REALTIME_SMOKE_SCREENSHOT=/mnt/data/workspace-se-codex/tmp/clawobserver-realtime-desk-hierarchy-fix.png npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `workstationOrientation: "all-desks-face-camera"`, `monitorDetail: "integrated-screen-keyboard-mouse"`, `workstationProportion: "wide-front-facing-workstation"`, `labelHierarchy: "small-monitor-top-metadata-tag"`, new `labelScaleHierarchy: "small-secondary-corner-badge"`, and new `deskAspect: 3`. Fit/readability margins stayed healthy (`prototype scene left/right 0.019`, `realtime scene left/right 0.024`; prototype label left/right `0.196/0.090`, realtime label left/right `0.198/0.092`), while idle perf guard remained within contract at `postSettleAnimationFrames: 4` on `/prototype` and `3` on `/`. Screenshot artifacts remained rich after the more aggressive geometry shift: prototype `fileBytes: 168715`, `brightPixels: 429313`, `darkPixels: 75649`, `midVariancePixels: 341346`, `uniqueBuckets: 4096`, `lumaSpread: 254.57`; realtime `fileBytes: 162851`, `brightPixels: 421835`, `darkPixels: 67462`, `midVariancePixels: 322031`, `uniqueBuckets: 4096`, `lumaSpread: 254.79`. Final review artifacts replaced the blocked screenshots at `/mnt/data/workspace-se-codex/tmp/clawobserver-prototype-desk-hierarchy-fix.png` and `/mnt/data/workspace-se-codex/tmp/clawobserver-realtime-desk-hierarchy-fix.png`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-11 command-center visual refinement pass after owner feedback: kept the existing shared R3F scene structure, repo-local Kenney CC0 asset pipeline, primitive fallback seam, compact full-width layout, elevated camera-facing label plates, open-center front label lane, opaque structures, and `AgentVisualState` contract intact while tightening style and orientation. `frontend/office-scene-prototype/src/config/workstationSlots.ts` now rotates all 12 desks so desk fronts and monitor screens face the camera consistently, which leaves the rear occupant side available for future seated/standing characters without changing the label billboard behavior. `frontend/office-scene-prototype/src/components/AgentDesk.tsx`, `OfficeShell.tsx`, `OfficeProps.tsx`, and `visualMapping.ts` now shift the scene away from warm cream/tan toy tones toward a deeper navy / midnight-blue environment, steel and cobalt furniture, cyan/electric-blue monitor and trim emissives, and sparse muted-orange accents. Monitor styling is stronger but still lightweight: visible screen planes, cyan edge glow, brighter screen treatment, and preserved keyboard/mouse props without adding structural clutter or breaking the idle-on-demand renderer contract. The scene metadata now declares `styleProfile: "toy-office-command-center"`, `styleReference: "quaternius-inspired-command-center-safe-emulation"`, `workstationOrientation: "all-desks-face-camera"`, and `monitorStyle: "screen-plane-cyan-edge"`.
- 2026-05-11 validation evidence for the command-center visual refinement pass:
  `npm --prefix frontend/office-scene-prototype test` => passed (`15` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-CqZPfOi2.js`, `styles-DOhokgfT.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0` for both routes. Prototype and realtime scene metadata both reported `styleProfile: "toy-office-command-center"`, `styleReference: "quaternius-inspired-command-center-safe-emulation"`, `workstationOrientation: "all-desks-face-camera"`, `monitorStyle: "screen-plane-cyan-edge"`, `frontLabelLaneClearance: "open-center"`, `deskStructureVisual: "opaque"`, `frameloop: "demand"`, and `performanceMode: "idle-on-demand"`. Fit and label margins stayed healthy (`prototype scene left/right 0.019`, `prototype label left/right 0.031`; `realtime scene left/right 0.024`, `realtime label left/right 0.036`). Idle perf guard stayed within contract at `postSettleAnimationFrames: 4` on both routes. Screenshot artifacts remained rich after the darker palette shift: prototype `fileBytes: 160366`, `brightPixels: 428105`, `darkPixels: 68752`, `midVariancePixels: 349966`, `uniqueBuckets: 4096`, `lumaSpread: 253.86`; realtime `fileBytes: 153997`, `brightPixels: 415863`, `darkPixels: 68293`, `midVariancePixels: 344181`, `uniqueBuckets: 4096`, `lumaSpread: 254.13`. Fresh screenshot artifacts: `tmp/prototype-browser-smoke.png`, `tmp/realtime-browser-smoke.png`.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-12 WorkAdventure dense-desk label overlap repair: kept the current WorkAdventure primary renderer, retained legacy `/prototype` 3D route, and left the `AgentVisualState` contract/assets/docs intact while tightening only the embedded tile-scene desk-card layout. `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` now defines per-desk hanger/nameplate X offsets and publishes `data-scene-front-label-lane-clearance="staggered-desk-nameplate-lane"`. `frontend/office-scene-prototype/src/styles.css` narrows the WorkAdventure desk-card footprint, shortens empty/offline helper copy, clamps secondary text, and compacts badge/task/Woka geometry so dense rows like Desk 7/8 and the open-desk clusters keep readable non-overlapping cards in the realtime browser smoke screenshot.
- 2026-05-12 overlap regression guard extension: `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now measures every realtime WorkAdventure nameplate rectangle in-browser and fails on any pairwise overlap. `frontend/office-scene-prototype/src/components/WorkAdventureScene.test.tsx` also pins the new staggered-label metadata/offset publication at component level.
- 2026-05-12 validation evidence for the WorkAdventure overlap repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`18` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-DtHhy7Lc.js`, `styles-DcyYaI6z.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Realtime scene metadata reported `frontLabelLaneClearance: "staggered-desk-nameplate-lane"`, `renderedNameplateCount: 12`, and `nameplateOverlaps: []`, directly validating that the dense WorkAdventure desk cards no longer intersect. Fresh screenshot evidence: `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`.
  `python3 -m pytest` => passed (`57` tests).
- 2026-05-12 full-agent WorkAdventure overflow blocker repair after owner rejection: `frontend/office-scene-prototype/src/lib/sceneState.ts` now returns explicit per-agent work/rest placements instead of a fixed 12-desk truncation, generates overflow markers inside only the approved work rectangles (`desk-bay-west`, `desk-bay-east`, `central-hotline`) and the approved rest rectangle (`lounge-silent-zone`), and records overflow counts so no `AgentVisualState` input is silently dropped. `frontend/office-scene-prototype/src/config/workAdventureLayout.ts` centralizes the fixed WorkAdventure anchor definitions. `frontend/office-scene-prototype/src/components/WorkAdventureScene.tsx` now renders every placement, publishes exact input/rendered/overflow agent counts, tags overflow markers, and keeps busy/error only on work markers while idle/offline render only on lounge markers. `frontend/office-scene-prototype/src/lib/liveOverviewSceneAdapter.ts`, `frontend/office-scene-prototype/src/lib/agentVisualAdapter.test.ts`, `clawobserver/models.py`, `clawobserver/runtime.py`, and `clawobserver/app.py` now preserve an optional per-agent `visual_status` hint through the runtime adapter boundary so mixed smoke fixtures can truthfully exercise busy, error, idle, and offline agents without changing the stable `AgentVisualState` contract.
- 2026-05-12 expanded fixture and regression coverage for the blocker repair: `frontend/office-scene-prototype/src/data/mockAgents.ts` now carries a larger mixed-status demo payload beyond the prior 4-5 agent sample. `tmp/realtime-mixed-fixture.json` now drives a 22-agent browser-smoke payload with `15` work-side busy/error agents and `7` rest-side idle/offline agents, including overflow on both sides. `frontend/office-scene-prototype/src/lib/sceneState.test.ts` now asserts generated overflow markers and no dropped work/rest agents; `frontend/office-scene-prototype/src/components/WorkAdventureScene.test.tsx` asserts rendered-agent count equals input count, busy/error never render as lounge badges, idle/offline never render as work desks, and overflow markers are explicitly tagged. `frontend/office-scene-prototype/scripts/prototypeBrowserSmoke.mjs` now fails unless realtime rendered count equals input count, work/rest zone counts stay correct by status, the selected detail panel is populated below the map, the custom background loads at `1264x848`, `/prototype` still renders, and nameplate overlap detection stays at zero even with overflow placements.
- 2026-05-12 validation evidence for the full-agent WorkAdventure blocker repair:
  `npm --prefix frontend/office-scene-prototype test` => passed (`22` tests). Existing `Multiple instances of Three.js being imported.` warning from `src/lib/sceneState.test.ts` remained non-fatal and unchanged.
  `npm --prefix frontend/office-scene-prototype run build` => passed; rebuilt `clawobserver/static/prototype/` with fresh bundle assets `prototype-C54Bj0rH.js`, `styles-BqGRpoiw.js`, and updated `realtime-scene-embed.js`.
  `npm --prefix frontend/office-scene-prototype run smoke:browser` => passed on isolated fixture-backed `http://127.0.0.1:8431/` and `http://127.0.0.1:8431/prototype` with `consoleErrors: 0`, `pageErrors: 0`, `failedRequests: 0`. Realtime scene metadata reported `renderedAgentCount: 22`, `inputAgentCount: 22`, `overflowAgentCount: 6`, `occupantCount: 15`, `restOccupantCount: 7`, `renderedDeskCount: 15`, `renderedLoungeCount: 7`, `renderedPlacementNodeCount: 22`, `invalidWorkPlacements: []`, `invalidRestPlacements: []`, `nameplateOverlaps: []`, `backgroundNaturalWidth: 1264`, `backgroundNaturalHeight: 848`, and `detailState: "selected"` with stable `detailStripLayout: "below-map-stable-horizontal"`. Prototype scene metadata remained healthy with retained legacy 3D markers, `canvasCount: 2`, `frameloop: "demand"`, and `performanceMode: "idle-on-demand"`. Fresh screenshot evidence: `tmp/realtime-browser-smoke.png`, `tmp/prototype-browser-smoke.png`, `tmp/prototype-avatar-preview-closeup.png`, `tmp/realtime-avatar-preview-closeup.png`.
  `python3 -m compileall clawobserver` => passed.
  `python3 -m pytest` => passed (`57` tests).
